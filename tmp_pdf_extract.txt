

=== PAGE 1 ===
import { useState, useCallback } from "react"; /* ─── Google Font ─────────────────────────────────────────────────────────── */ const FontLink = () => ( <style>{` @import url('https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800&family=JetBrains+Mono:wght@400;600&display=swap'); *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; } body { background: #f5f4f0; } ::-webkit-scrollbar { width: 5px; height: 5px; } ::-webkit-scrollbar-track { background: #f0efe9; } ::-webkit-scrollbar-thumb { background: #c8c4b8; border-radius: 10px; } input:focus, select:focus, textarea:focus { outline: none; border-color: #2563eb !important; box-shadow: 0 0 0 3px #2563eb18; } button:active { transform: scale(0.97); } @keyframes fadeIn { from { opacity:0; transform:translateY(8px); } to { opacity:1; transform:translateY(0); } } @keyframes toastIn { from { opacity:0; transform:translateY(-16px) scale(.95); } to { opacity:1; transform:translateY(0) scale(1); } } @keyframes slideRight { from { transform:translateX(-100%); } to { transform:translateX(0); } } /* Bottom nav for mobile */ .bottom-nav { display:none; } @media (max-width: 640px) { .bottom-nav { display:flex !important; } .desktop-sidebar { display:none !important; } .page-pad { padding: 16px 14px !important; } .form-grid-2col { grid-template-columns: 1fr !important; } .hide-mobile { display:none !important; } .stats-grid { grid-template-columns: 1fr 1fr !important; } .two-col { grid-template-columns: 1fr !important; } } `}</style> ); /* ─── Utilidades ──────────────────────────────────────────────────────────── */ const genCode = (catName="", brandName="") => { const slug = s => s.normalize("NFD").replace(/[\u0300-\u036f]/g,"") .toUpperCase().replace(/[^A-Z0-9]/g,"").slice(0,3).padEnd(3,"X"); const catPart = slug(catName || "GEN"); const brandPart = slug(brandName || "PRD"); const seq = Math.floor(100 + Math.random() * 900); // 3 digits return `${catPart}-${brandPart}-${seq}`; }; const fmtCLP = (n) => `$${Number(n || 0).toLocaleString("es-CL")}`; const today = () => new Date().toISOString().split("T")[0]; /* ─── Roles (2 niveles) ───────────────────────────────────────────────────── */ const ROLES = {


=== PAGE 2 ===
admin: { label: "Administrador", color: "#2563eb", bg: "#eff6ff" }, vendedor: { label: "Vendedor", color: "#059669", bg: "#ecfdf5" }, }; const isAdmin = (user) => user?.role === "admin"; /* ─── Datos iniciales ─────────────────────────────────────────────────────── */ const INIT_USERS = [ { id:"u1", name:"Administrador General", username:"admin", password:"admin123", role:"admin", active:true }, { id:"u2", name:"María González", username:"mgonzalez", password:"vend123", role:"vendedor", active:true }, ]; const INIT_CATS = [ { id:"cat1", name:"Teléfonos", icon:" " }, { id:"cat2", name:"Parlantes", icon:" " }, { id:"cat3", name:"Teclados", icon:" " }, { id:"cat4", name:"Accesorios", icon:" " }, ]; const INIT_BRANDS = [ { id:"br1", catId:"cat1", name:"Samsung" }, { id:"br2", catId:"cat1", name:"Apple" }, { id:"br3", catId:"cat1", name:"Xiaomi" }, { id:"br4", catId:"cat2", name:"JBL" }, { id:"br5", catId:"cat2", name:"Sony" }, { id:"br6", catId:"cat3", name:"Logitech" }, ]; const INIT_PRODUCTS = [ { id:"p1", code:"3841920", catId:"cat1", brandId:"br1", name:"Samsung Galaxy A54", description:"128GB · Negro · Dual SIM", costPrice:280000, salePrice:350000, minPrice:320000, stock:5, imeis:["354678901234567","354678901234568"], lowStockAlert:2, isPhone:true }, { id:"p2", code:"7291045", catId:"cat2", brandId:"br4", name:"JBL Flip 6", description:"Parlante Bluetooth portátil resistente al agua", costPrice:55000, salePrice:89000, minPrice:75000, stock:12, imeis:[], lowStockAlert:3, isPhone:false }, { id:"p3", code:"5509183", catId:"cat1", brandId:"br2", name:"iPhone 14 128GB", description:"Negro - Desbloqueado - Garantía", costPrice:700000, salePrice:950000, minPrice:900000, stock:1, imeis:["358921012345678"], lowStockAlert:2, isPhone:true }, { id:"p4", code:"2204817", catId:"cat3", brandId:"br6", name:"Logitech MX Keys", description:"Teclado inalámbrico retroiluminado", costPrice:85000, salePrice:130000, minPrice:115000, stock:0, imeis:[], lowStockAlert:2, isPhone:false }, ]; /* ─── Storage hook ────────────────────────────────────────────────────────── */ const useStore = (key, init) => { const [val, setVal] = useState(() => { try { const s = localStorage.getItem(key); return s ? JSON.parse(s) : init; } catch { return init; }


=== PAGE 3 ===
}); const update = useCallback((v) => { setVal(v); try { localStorage.setItem(key, JSON.stringify(v)); } catch {} }, [key]); return [val, update]; }; /* ─── Export CSV ──────────────────────────────────────────────────────────── */ const exportCSV = (sales, products, users, dateFrom, dateTo, expenses=[]) => { const filtered = sales.filter(s => s.date >= dateFrom && s.date <= dateTo); const filteredExp = expenses.filter(e => e.date >= dateFrom && e.date <= dateTo); const rows = [["Fecha","Tipo","Código","Producto/Descripción","Categoría","Vendedor","Precio Adquisición","Precio Venta","Ganancia","IMEI/Serie"]]; filtered.forEach(s => { const p = products.find(x => x.id === s.productId); const u = users.find(x => x.id === s.userId); rows.push([s.date,"Venta", p?.code||"", p?.name||"", s.catName||"", u?.name||"", p?.costPrice||0, s.salePrice, s.salePrice-(p?.costPrice||0), s.imei||"-"]); }); filteredExp.forEach(e => { const u = users.find(x => x.id === e.userId); rows.push([e.date,"Gasto","",e.description,"",u?.name||"","",`-${e.amount}`,`-${e.amount}`,""]); }); const csv = rows.map(r => r.join(";")).join("\n"); const a = document.createElement("a"); a.href = URL.createObjectURL(new Blob(["\uFEFF"+csv], {type:"text/csv;charset=utf-8;"})); a.download = `movimientos_${dateFrom}_${dateTo}.csv`; a.click(); }; /* ─── NOTIFICACIONES ────────────────────────────────────────────────────── */ function NotifBell({notifs, setNotifs, session}) { const [open, setOpen] = useState(false); const mine = (notifs||[]).filter(n=>n.userId===session.id); const unread = mine.filter(n=>!n.read).length; const markAll = () => setNotifs((notifs||[]).map(n=>n.userId===session.id?{...n,read:true}:n)); const dismiss = (id) => setNotifs((notifs||[]).filter(n=>n.id!==id)); return ( <div style={{position:"relative"}}> <button onClick={()=>{setOpen(o=>!o); if(!open) markAll();}} style={{background:"none",border:"none",fontSize:20,cursor:"pointer",padding:"2px 4px",lineHeight:1,position:"relative"}}> {unread>0&&<span style={{position:"absolute",top:-3,right:-3,background:"#dc2626",color:"#fff",borderRadius:"50%",width:15,height:15,fontSize:9,fontWeight:700,display:"flex",alignItems:"center",justifyContent:"center",lineHeight:1}}>{unread>9?"9+":unread}</span>} </button> {open&&( <div style={{position:"absolute",right:0,top:34,width:300,background:"#fff",border:"1px solid #e5e7eb",borderRadius:12,boxShadow:"0 8px 24px rgba(0,0,0,.12)",zIndex:999,maxHeight:340,overflowY:"auto"}}> <div style={{padding:"10px 14px",borderBottom:"1px solid #f3f4f6",fontWeight:700,fontSize:13,color:"#111827"}}>Notificaciones</div>


=== PAGE 4 ===
{mine.length===0&&<div style={{padding:20,textAlign:"center",color:"#9ca3af",fontSize:13}}>Sin notificaciones</div>} {mine.slice().reverse().map(n=>( <div key={n.id} style={{padding:"10px 14px",borderBottom:"1px solid #f9fafb",display:"flex",gap:8,alignItems:"flex-start",background:n.read?"#fff":"#f0f9ff"}}> <span style={{fontSize:16,flexShrink:0}}>{n.type==="credit_pending"?" ":n.type==="week_reminder"?" ":n.type==="card_pending"?" ":" "}</span> <div style={{flex:1}}> <div style={{fontSize:12,color:"#111827",lineHeight:1.4}}>{n.msg}</div> {n.dueDate&&<div style={{fontSize:10,color:"#9ca3af",marginTop:2}}>Vence: {n.dueDate}</div>} <div style={{fontSize:10,color:"#9ca3af",marginTop:1}}>{new Date(n.createdAt).toLocaleString("es-CL",{day:"2-digit",month:"2-digit",hour:"2-digit",minute:"2-digit"})}</div> </div> <button onClick={e=>{e.stopPropagation();dismiss(n.id);}} style={{background:"none",border:"none",cursor:"pointer",color:"#9ca3af",fontSize:13,padding:"2px 4px",flexShrink:0}}>✕</button> </div> ))} </div> )} </div> ); } /* ════════════════════════════════════════════════════════════════════════════ APP ROOT ════════════════════════════════════════════════════════════════════════════ */ export default function App() { const [users, setUsers] = useStore("iv2_users", INIT_USERS); const [cats, setCats] = useStore("iv2_cats", INIT_CATS); const [brands, setBrands] = useStore("iv2_brands", INIT_BRANDS); const [products, setProducts] = useStore("iv2_products", INIT_PRODUCTS); const [sales, setSales] = useStore("iv2_sales", []); const [expenses, setExpenses] = useStore("iv2_expenses", []); const [requests, setRequests] = useStore("iv2_requests", []); const [creditSales,setCreditSales]= useStore("iv2_credits", []); const [cardSales, setCardSales] = useStore("iv2_cards", []); const [notifs, setNotifs] = useStore("iv2_notifs", []); const [session, setSession] = useState(()=>{ try { const s=localStorage.getItem("iv2_session"); return s?JSON.parse(s):null; } catch { return null; } }); const [view, setView] = useState("dashboard"); const [toast, setToast] = useState(null); const [collapsed, setCollapsed] = useState(false); const [drawerOpen, setDrawerOpen] = useState(false); const notify = (msg, type="ok") => { setToast({msg,type}); setTimeout(()=>setToast(null),3200); }; const login = (user, remember) => { setSession(user); if (remember) { try { localStorage.setItem("iv2_session", JSON.stringify(user)); } catch {} } }; const logout = () => {


=== PAGE 5 ===
setSession(null); try { localStorage.removeItem("iv2_session"); } catch {} }; if (!session) return <Login users={users} onLogin={login} />; // ── Verificar horario para no-admins ─────────────────────────────────── if (!isAdmin(session)) { const currentUser = users.find(u=>u.id===session.id); if (currentUser?.scheduleStart && currentUser?.scheduleEnd) { const now = new Date(); const [sh,sm]=currentUser.scheduleStart.split(":").map(Number); const [eh,em]=currentUser.scheduleEnd.split(":").map(Number); const nowMins=now.getHours()*60+now.getMinutes(); const startMins=sh*60+sm, endMins=eh*60+em; const inSchedule = startMins<=endMins ? nowMins>=startMins && nowMins<endMins : nowMins>=startMins || nowMins<endMins; // rango que cruza medianoche if (!inSchedule) { return ( <div style={{minHeight:"100vh",display:"flex",alignItems:"center",justifyContent:"center",background:"#f9fafb",padding:24}}> <div style={{background:"#fff",border:"1px solid #e5e7eb",borderRadius:16,padding:"40px 32px",maxWidth:380,textAlign:"center",boxShadow:"0 4px 24px rgba(0,0,0,.08)"}}> <div style={{fontSize:48,marginBottom:16}}> </div> <h2 style={{fontWeight:800,fontSize:18,color:"#111827",marginBottom:8}}>Acceso fuera de horario</h2> <p style={{fontSize:13,color:"#6b7280",marginBottom:16}}> Tu horario de acceso es de <strong>{currentUser.scheduleStart}</strong> a <strong>{currentUser.scheduleEnd}</strong>. Por favor ingresa dentro de tu horario asignado. </p> <button onClick={logout} style={{background:"#2563eb",color:"#fff",border:"none",borderRadius:8,padding:"10px 24px",fontWeight:600,cursor:"pointer",fontSize:13}}> Cerrar sesión </button> </div> </div> ); } } } const admin = isAdmin(session); const NAV = [ { id:"dashboard", icon:"▦", label:"Inicio" }, { id:"inventory", icon:"◫", label:"Inventario" }, { id:"sales", icon:"◈", label:"Ventas" }, { id:"lowstock", icon:"◎", label:"Stock Bajo" }, { id:"historial", icon:"◷", label:"Historial" }, ...(admin ? [


=== PAGE 6 ===
{ id:"reports", icon:"◉", label:"Reportes" }, { id:"staff", icon:"◑", label:"Personal" }, ] : []), ]; const navigate = (id) => { setView(id); setDrawerOpen(false); }; // ── Shared sidebar inner content ────────────────────────────────────────── const SidebarInner = ({ mobile }) => ( <> {/* Header row */} <div style={{display:"flex",alignItems:"center",justifyContent:"space-between",padding:"0 4px 14px",borderBottom:"1px solid #f3f4f6",marginBottom:8,minHeight:46}}> {(!collapsed||mobile) && ( <div style={{display:"flex",alignItems:"center",gap:10,overflow:"hidden",flex:1}}> <span style={{fontSize:24,flexShrink:0}}> </span> <div style={{overflow:"hidden"}}> <div style={S.brandName}>InvenTrack</div> <div style={S.brandSub}>Gestión de Inventario</div> </div> </div> )} {collapsed && !mobile && <span style={{fontSize:22,margin:"0 auto"}}> </span>} {/* Desktop collapse toggle */} {!mobile && ( <button onClick={()=>setCollapsed(c=>!c)} style={S.toggleBtn} title={collapsed?"Expandir panel":"Colapsar panel"}> {collapsed ? "▶" : "◀"} </button> )} {/* Mobile close */} {mobile && ( <button onClick={()=>setDrawerOpen(false)} style={{...S.toggleBtn,marginLeft:8}}>✕</button> )} </div> {/* User card */} {(!collapsed||mobile) ? ( <div style={S.userCard}> <div style={S.avatar}>{session.name[0]}</div> <div style={{minWidth:0,flex:1}}> <div style={{fontWeight:700,fontSize:12,color:"#111827",overflow:"hidden",textOverflow:"ellipsis",whiteSpace:"nowrap"}}>{session.name}</div> <span style={{...S.rolePill,background:ROLES[session.role].bg,color:ROLES[session.role].color}}>{ROLES[session.role].label}</span> </div> <NotifBell notifs={notifs} setNotifs={setNotifs} session={session}/> </div> ) : ( <div style={{display:"flex",flexDirection:"column",alignItems:"center",gap:6,marginBottom:12}}> <div style={{...S.avatar,width:32,height:32,fontSize:13}}>{session.name[0]}</div>


=== PAGE 7 ===
<NotifBell notifs={notifs} setNotifs={setNotifs} session={session}/> </div> )} {/* Nav items */} <nav style={{display:"flex",flexDirection:"column",gap:2,flex:1}}> {NAV.map(n=>{ const pending = n.id==="inventory" && admin && (requests.filter(r=>r.status==="pending").length + creditSales.filter(c=>c.status==="pending").length + cardSales.filter(c=>c.status==="pending").length); return ( <button key={n.id} style={{ ...S.navBtn, ...(view===n.id?S.navActive:{}), justifyContent: (collapsed&&!mobile)?"center":"flex-start", padding: (collapsed&&!mobile)?"10px 0":"9px 12px", }} onClick={()=>navigate(n.id)} title={(collapsed&&!mobile)?n.label:undefined}> <span style={{fontSize:(collapsed&&!mobile)?18:14,width:(collapsed&&!mobile)?"100%":18,textAlign:"center",position:"relative",display:"inline-block"}}> {n.icon} {!!pending&&<span style={{position:"absolute",top:-4,right:-6,background:"#dc2626",color:"#fff",borderRadius:"50%",width:14,height:14,fontSize:9,fontWeight:700,display:"flex",alignItems:"center",justifyContent:"center",lineHeight:1}}>{pending>9?"9+":pending}</span>} </span> {(!collapsed||mobile) && <span style={{flex:1}}>{n.label}</span>} {(!collapsed||mobile) && !!pending && <span style={{background:"#dc2626",color:"#fff",borderRadius:10,padding:"1px 7px",fontSize:10,fontWeight:700}}>{pending}</span>} </button> );})} </nav> {/* Logout */} <button style={{...S.logoutBtn,textAlign:(collapsed&&!mobile)?"center":"left",padding:(collapsed&&!mobile)?"10px 0":"10px 12px"}} onClick={()=>logout()}> {(collapsed&&!mobile) ? " " : "← Cerrar sesión"} </button> </> ); return ( <div style={S.root}> <FontLink /> {/* ── DESKTOP SIDEBAR ─────────────────────────────────────────────── */} <aside className="desktop-sidebar" style={{...S.sidebar, width:collapsed?64:232, transition:"width .2s ease", flexDirection:"column", display:"flex"}}> <SidebarInner mobile={false} /> </aside> {/* ── MOBILE DRAWER + OVERLAY ─────────────────────────────────────── */}


=== PAGE 8 ===
{drawerOpen && ( <div style={S.drawerOverlay} onClick={()=>setDrawerOpen(false)}> <aside style={S.drawer} onClick={e=>e.stopPropagation()}> <SidebarInner mobile={true} /> </aside> </div> )} {/* ── MAIN CONTENT ────────────────────────────────────────────────── */} <main style={S.main}> {/* Mobile top bar */} <div className="bottom-nav" style={S.mobileTopBar}> <button style={S.mobileMenuBtn} onClick={()=>setDrawerOpen(true)}>☰</button> <span style={{fontWeight:800,fontSize:15,color:"#111827",letterSpacing:"-.02em"}}> InvenTrack</span> <div style={{display:"flex",alignItems:"center",gap:8}}> <NotifBell notifs={notifs} setNotifs={setNotifs} session={session}/> <div style={{...S.avatar,width:30,height:30,fontSize:13,flexShrink:0}}>{session.name[0]}</div> </div> </div> {toast && <Toast msg={toast.msg} type={toast.type} />} <div style={{animation:"fadeIn .25s ease", paddingBottom:72}} key={view}> {view==="dashboard" && <Dashboard products={products} sales={sales} cats={cats} expenses={expenses} session={session} />} {view==="inventory" && <Inventory products={products} setProducts={setProducts} cats={cats} setCats={setCats} brands={brands} setBrands={setBrands} admin={admin} session={session} requests={requests} setRequests={setRequests} sales={sales} creditSales={creditSales} setCreditSales={setCreditSales} cardSales={cardSales} setCardSales={setCardSales} setSales={setSales} notify={notify} />} {view==="sales" && <Sales products={products} setProducts={setProducts} sales={sales} setSales={setSales} expenses={expenses} setExpenses={setExpenses} creditSales={creditSales} setCreditSales={setCreditSales} cardSales={cardSales} setCardSales={setCardSales} notifs={notifs} setNotifs={setNotifs} users={users} cats={cats} session={session} notify={notify} />} {view==="lowstock" && <LowStock products={products} cats={cats} brands={brands} />} {view==="historial" && <Historial sales={sales} setSales={setSales} products={products} setProducts={setProducts} session={session} admin={admin} />} {view==="reports" && admin && <Reports sales={sales} products={products} users={users} cats={cats} expenses={expenses} creditSales={creditSales} cardSales={cardSales} />} {view==="staff" && admin && <Staff users={users} setUsers={setUsers} notify={notify} />} </div> {/* Mobile bottom navigation */} <nav className="bottom-nav" style={S.mobileNav}> {NAV.slice(0,5).map(n=>( <button key={n.id} style={{...S.mobileNavBtn,...(view===n.id?S.mobileNavActive:{})}} onClick={()=>navigate(n.id)}> <span style={{fontSize:20}}>{n.icon}</span> <span style={{fontSize:9,marginTop:1,fontWeight:view===n.id?700:500}}>{n.label}</span> </button> ))} </nav> </main> </div> ); } /* ─── LOGIN ──────────────────────────────────────────────────────────────── */


=== PAGE 9 ===
function Login({ users, onLogin }) { const [u,setU]=useState(""); const [p,setP]=useState(""); const [err,setErr]=useState(""); const [loading,setLoading]=useState(false); const [remember,setRemember]=useState(true); const submit = () => { setLoading(true); setTimeout(()=>{ const found = users.find(x=>x.username===u&&x.password===p&&x.active); if (found) onLogin(found, remember); else { setErr("Usuario o contraseña incorrectos"); setLoading(false); } },350); }; return ( <div style={S.loginBg}> <FontLink /> <div style={S.loginWrap}> <div style={S.loginLeft}> <div> <span style={{fontSize:44}}> </span> <h1 style={S.loginBig}>InvenTrack</h1> <p style={{color:"#93c5fd",fontSize:14,lineHeight:1.7,marginTop:8}}>Sistema completo de gestión de inventario para tu comercio</p> </div> <div style={{display:"flex",flexDirection:"column",gap:10,marginTop:36}}> {["Control de stock en tiempo real","Gestión de IMEIs para teléfonos","Registro y exportación de ventas","Alertas de stock bajo automáticas"].map(f=>( <div key={f} style={{display:"flex",alignItems:"center",gap:10,color:"#bfdbfe",fontSize:13}}> <div style={{width:6,height:6,borderRadius:"50%",background:"#60a5fa",flexShrink:0}} /> {f} </div> ))} </div> </div> <div style={S.loginRight}> <div style={S.loginCard}> <h2 style={{fontSize:22,fontWeight:800,color:"#111827",letterSpacing:"-.03em",marginBottom:4}}>Bienvenido</h2> <p style={{fontSize:13,color:"#9ca3af",marginBottom:22}}>Ingresa tus credenciales para continuar</p> <label style={S.label}>Usuario</label> <input style={S.input} value={u} onChange={e=>{setU(e.target.value);setErr("")}} placeholder="nombre de usuario" onKeyDown={e=>e.key==="Enter"&&submit()} /> <label style={{...S.label,marginTop:14}}>Contraseña</label> <input style={S.input} type="password" value={p} onChange={e=>{setP(e.target.value);setErr("")}} placeholder="••••••••" onKeyDown={e=>e.key==="Enter"&&submit()} /> {err&&<p style={{color:"#dc2626",fontSize:12,marginTop:6}}>{err}</p>} <label style={{display:"flex",alignItems:"center",gap:8,cursor:"pointer",marginTop:14,padding:"10px 12px",background:"#f0f9ff",borderRadius:8,border:"1px solid #bfdbfe"}}> <input type="checkbox" checked={remember} onChange={e=>setRemember(e.target.checked)} style={{width:16,height:16,accentColor:"#2563eb",flexShrink:0}} /> <div> <div style={{fontSize:13,fontWeight:600,color:"#1d4ed8"}}>Mantener sesión iniciada</div> <div style={{fontSize:11,color:"#6b7280",marginTop:1}}>No deberás iniciar sesión cada vez que abras la app</div> </div> </label> <button style={{...S.btnPrimary,width:"100%",marginTop:14,opacity:loading?.7:1}} onClick={submit} disabled={loading}> {loading?"Verificando…":"Ingresar →"}


=== PAGE 10 ===
</button> <div style={{marginTop:20,padding:"12px 14px",background:"#f9fafb",borderRadius:10}}> <div style={{fontSize:10,fontWeight:700,color:"#9ca3af",textTransform:"uppercase",marginBottom:6}}>Accesos demo</div> <code style={{display:"block",fontFamily:"'JetBrains Mono',monospace",fontSize:11,color:"#374151",background:"#f3f4f6",padding:"3px 8px",borderRadius:5,marginBottom:4}}>admin / admin123</code> <code style={{display:"block",fontFamily:"'JetBrains Mono',monospace",fontSize:11,color:"#374151",background:"#f3f4f6",padding:"3px 8px",borderRadius:5}}>mgonzalez / vend123</code> </div> </div> </div> </div> </div> ); } /* ─── DASHBOARD ──────────────────────────────────────────────────────────── */ function Dashboard({ products, sales, cats, expenses, session }) { const admin = isAdmin(session); const t=today(); // Vendedor solo ve sus propias ventas del día const todaySales = admin ? sales.filter(s=>s.date===t) : sales.filter(s=>s.date===t && s.userId===session.id); const revenue=todaySales.reduce((a,s)=>a+s.salePrice,0); const costs=todaySales.reduce((a,s)=>a+(s.costPrice||0),0); const todayExpenses=expenses.filter(e=>e.date===t); const totalExpenses=todayExpenses.reduce((a,e)=>a+e.amount,0); const net=revenue-costs-totalExpenses; const totalStock=products.reduce((a,p)=>a+p.stock,0); const low=products.filter(p=>p.stock>0&&p.stock<=p.lowStockAlert).length; const out=products.filter(p=>p.stock===0).length; return ( <Page title="Dashboard" sub={new Date().toLocaleDateString("es-CL",{weekday:"long",year:"numeric",month:"long",day:"numeric"})}> <div style={S.statsGrid}> <StatCard color="#2563eb" icon=" " label={admin?"Ventas hoy":"Mis ventas hoy"} val={todaySales.length} sub={fmtCLP(revenue)+" ingresos"} /> {admin && <StatCard color="#dc2626" icon=" " label="Gastos hoy" val={todayExpenses.length} sub={fmtCLP(totalExpenses)+" gastado"} />} {admin && <StatCard color={net>=0?"#059669":"#dc2626"} icon=" " label="Ganancias del día" val={fmtCLP(net)} sub={net>=0?"balance positivo":"balance negativo"} />} <StatCard color="#059669" icon=" " label="Unidades en stock" val={totalStock} sub={products.length+" productos distintos"} /> <StatCard color="#d97706" icon=" " label="Stock reducido" val={low} sub={out+" agotados"} /> {admin && <StatCard color="#7c3aed" icon=" " label="Categorías" val={cats.length} sub="tipos de producto" />} </div> <div style={S.twoCol}> <Card title="Por Categoría"> {cats.map(c=>{ const ps=products.filter(p=>p.catId===c.id); return ( <div key={c.id} style={S.row}> <span style={{fontSize:22}}>{c.icon}</span> <span style={{flex:1,fontSize:14,color:"#374151",fontWeight:500}}>{c.name}</span>


=== PAGE 11 ===
<Pill>{ps.length} prod.</Pill> <Pill color="#dbeafe" tc="#1d4ed8">{ps.reduce((a,p)=>a+p.stock,0)} uds.</Pill> </div> ); })} </Card> <Card title="Últimas Ventas"> {sales.length===0 ? <p style={S.empty}>Sin ventas registradas aún</p> : sales.slice(-7).reverse().map(s=>( <div key={s.id} style={S.row}> <span style={{fontSize:11,color:"#9ca3af",width:72,flexShrink:0}}>{s.date}</span> <span style={{flex:1,fontSize:13,color:"#374151",overflow:"hidden",textOverflow:"ellipsis",whiteSpace:"nowrap"}}>{s.productName}</span> <span style={{fontSize:13,fontWeight:700,color:"#059669"}}>{fmtCLP(s.salePrice)}</span> </div> )) } </Card> </div> </Page> ); } function StatCard({color,icon,label,val,sub}) { return ( <div style={{...S.statCard,borderTop:`3px solid ${color}`}}> <span style={{fontSize:28}}>{icon}</span> <div style={{fontSize:28,fontWeight:800,color,lineHeight:1.1,marginTop:4}}>{val}</div> <div style={{fontSize:13,fontWeight:600,color:"#374151"}}>{label}</div> <div style={{fontSize:11,color:"#9ca3af",marginTop:2}}>{sub}</div> </div> ); } /* ─── INVENTARIO ─────────────────────────────────────────────────────────── */ function Inventory({ products, setProducts, cats, setCats, brands, setBrands, admin, session, requests, setRequests, sales, creditSales, setCreditSales, cardSales, setCardSales, setSales, notify }) { const [tab,setTab]=useState("productos"); const [search,setSearch]=useState(""); const [catF,setCatF]=useState(""); const [modal,setModal]=useState(null); const [form,setForm]=useState({}); const [approveForm,setApproveForm]=useState({}); // form para aprobación (precio adquisición) const catName = id=>cats.find(c=>c.id===id)?.name||""; const brandName = id=>brands.find(b=>b.id===id)?.name||""; const pendingRequests = requests.filter(r=>r.status==="pending"); const pendingCreditReqs = (creditSales||[]).filter(c=>c.status==="pending"); const pendingCardReqs = (cardSales||[]).filter(c=>c.status==="pending"); const totalPending = pendingRequests.length + pendingCreditReqs.length + pendingCardReqs.length;


=== PAGE 12 ===
// Aprobar/denegar crédito desde Inventario const approveCreditInv = (cr) => { const approvedDate = today(); const p = products.find(x=>x.id===cr.productId); const cat = cats.find(c=>c.id===p?.catId); setSales(prev=>[...prev,{ id:"s"+Date.now(), productId:cr.productId, productName:cr.productName, catName:cat?.name||"Teléfonos", userId:cr.requestedBy, userName:cr.requestedByName, date:approvedDate, salePrice:cr.adminReceives, costPrice:cr.costPrice||0, salePriceOriginal:cr.salePrice, imei:cr.imei||null, isCredit:true }]); setCreditSales(prev=>prev.map(c=>c.id===cr.id?{...c,status:"approved",approvedAt:approvedDate}:c)); notify("✓ Venta a crédito aprobada"); }; const denyCreditInv = (cr) => { setProducts(products.map(p=>{ if(p.id!==cr.productId)return p; const imeis=cr.imei?[...p.imeis,cr.imei]:p.imeis; return {...p,stock:p.stock+1,imeis}; })); setCreditSales(prev=>prev.map(c=>c.id===cr.id?{...c,status:"denied"}:c)); notify("Solicitud de crédito denegada","warn"); }; const approveCardInv = (cd) => { const approvedDate = today(); const p = products.find(x=>x.id===cd.productId); const cat = cats.find(c=>c.id===p?.catId); setSales(prev=>[...prev,{ id:"s"+Date.now(), productId:cd.productId, productName:cd.productName, catName:cat?.name||"", userId:cd.requestedBy, userName:cd.requestedByName, date:approvedDate, salePrice:cd.salePrice, costPrice:cd.costPrice||0, imei:cd.imei||null, isCard:true, payMethod:"tarjeta", }]); setCardSales(prev=>prev.map(c=>c.id===cd.id?{...c,status:"approved",approvedAt:approvedDate}:c)); notify("✓ Venta con tarjeta aprobada"); }; const denyCardInv = (cd) => { setProducts(products.map(p=>{ if(p.id!==cd.productId)return p; const imeis=cd.imei?[...p.imeis,cd.imei]:p.imeis; return {...p,stock:p.stock+1,imeis}; })); setCardSales(prev=>prev.map(c=>c.id===cd.id?{...c,status:"denied"}:c)); notify("Venta con tarjeta denegada","warn"); };


=== PAGE 13 ===
const filtered = products.filter(p=> (p.name.toLowerCase().includes(search.toLowerCase())||p.code.includes(search)|| p.description.toLowerCase().includes(search.toLowerCase())|| p.imeis.some(i=>i.includes(search)))&&(!catF||p.catId===catF) ); // Admin: abre formulario completo para nuevo producto const openAdd = ()=>{ setForm({code:genCode(),catId:"",brandId:"",name:"",description:"",costPrice:"",salePrice:"",minPrice:"",stock:"",lowStockAlert:2,isPhone:false,imeis:[]}); setModal("form"); }; const openEdit = p=>{ setForm({...p,imeis:[...p.imeis]}); setModal("form"); }; const openImei = p=>{ setForm({...p,newImei:""}); setModal("imei"); }; // Vendedor: envía solicitud de nuevo producto (sin precio adquisición) const openRequest = ()=>{ setForm({code:genCode(),catId:"",brandId:"",name:"",description:"",salePrice:"",minPrice:"",stock:"",lowStockAlert:2,isPhone:false,imeis:[]}); setModal("request"); }; const saveAdmin = ()=>{ if (!form.name||!form.catId) { notify("Completa nombre y categoría","err"); return; } if (form.id) setProducts(products.map(p=>p.id===form.id?form:p)); else setProducts([...products,{...form,id:"p"+Date.now(),imeis:form.imeis||[]}]); notify(form.id?"Producto actualizado":"Producto creado"); setModal(null); }; const sendRequest = ()=>{ if (!form.name||!form.catId) { notify("Completa nombre y categoría","err"); return; } const req = { id:"req"+Date.now(), status:"pending", createdAt:Date.now(), requestedBy:session.id, requestedByName:session.name, product:{...form, id:null, imeis:form.imeis||[], costPrice:0}, }; setRequests([...requests, req]); notify("✓ Solicitud enviada al administrador"); setModal(null); }; const del = id=>{ setProducts(products.filter(p=>p.id!==id)); notify("Producto eliminado"); }; // Admin: abre modal de aprobación const openApprove = (req)=>{ setApproveForm({reqId:req.id, costPrice:"", ...req.product}); setModal("approve"); }; const approveRequest = ()=>{ if (!approveForm.costPrice||+approveForm.costPrice<=0) { notify("Ingresa el precio de adquisición","err"); return; } const newProduct = {...approveForm, id:"p"+Date.now(), costPrice:+approveForm.costPrice, imeis:approveForm.imeis||[]}; delete newProduct.reqId; setProducts([...products, newProduct]);


=== PAGE 14 ===
setRequests(requests.map(r=>r.id===approveForm.reqId?{...r,status:"approved",resolvedAt:Date.now()}:r)); notify("✓ Producto aprobado y agregado al inventario"); setModal(null); }; const rejectRequest = (reqId)=>{ setRequests(requests.map(r=>r.id===reqId?{...r,status:"rejected",resolvedAt:Date.now()}:r)); notify("Solicitud rechazada","warn"); setModal(null); }; const addImei = ()=>{ if (!form.newImei) return; const imei = form.newImei.trim(); // Verificar duplicado en todos los productos existentes const existsInProducts = products.some(p=>p.id!==form.id && p.imeis.includes(imei)); // Verificar duplicado en el formulario actual const existsInForm = form.imeis.includes(imei); // Verificar duplicado en ventas históricas const existsInSales = (typeof sales !== "undefined" ? sales : []).some(s=>s.imei===imei); if(existsInForm){notify("Este IMEI ya está en la lista del producto","err");return;} if(existsInProducts){notify("Este IMEI ya existe en otro producto del inventario","err");return;} if(existsInSales){notify("Este IMEI ya fue vendido anteriormente","err");return;} const imeis=[...form.imeis,imei]; const upd={...form,imeis,newImei:"",stock:imeis.length}; setForm(upd); setProducts(products.map(p=>p.id===form.id?{...p,imeis,stock:imeis.length}:p)); notify("IMEI agregado"); }; const removeImei = imei=>{ const imeis=form.imeis.filter(i=>i!==imei); setForm({...form,imeis,stock:imeis.length}); setProducts(products.map(p=>p.id===form.id?{...p,imeis,stock:imeis.length}:p)); }; const tabList = admin ? ["productos","solicitudes","categorías","marcas"] : ["productos","mis solicitudes","categorías","marcas"]; return ( <Page title="Inventario" sub={`${products.length} productos · ${products.reduce((a,p)=>a+p.stock,0)} unidades`} action={ admin ? <button style={S.btnPrimary} onClick={openAdd}>+ Nuevo Producto</button> : <button style={S.btnPrimary} onClick={openRequest}>+ Solicitar Producto</button> }> <div style={S.tabs}> {tabList.map(t=>{


=== PAGE 15 ===
const isSolicitudes = t==="solicitudes"; return ( <button key={t} style={{...S.tab,...(tab===t?S.tabOn:{}),position:"relative"}} onClick={()=>setTab(t)}> {t.charAt(0).toUpperCase()+t.slice(1)} {isSolicitudes && totalPending>0 && ( <span style={{marginLeft:6,background:"#dc2626",color:"#fff",borderRadius:10,padding:"1px 6px",fontSize:10,fontWeight:700}}>{totalPending}</span> )} </button> ); })} </div> {/* ── Tab Productos ── */} {tab==="productos"&&<> <div style={{display:"flex",gap:8,marginBottom:14,flexWrap:"wrap"}}> <input style={{...S.input,flex:1,maxWidth:340}} placeholder="Buscar nombre, código, IMEI…" value={search} onChange={e=>setSearch(e.target.value)} /> <select style={{...S.select,width:200}} value={catF} onChange={e=>setCatF(e.target.value)}> <option value="">Todas las categorías</option> {cats.map(c=><option key={c.id} value={c.id}>{c.icon} {c.name}</option>)} </select> </div> <div style={S.tableWrap}> <table style={S.table}> <thead><tr style={S.thead}>{["Código","Producto","Categoría","Marca","Stock","Precio Venta",""].map(h=><th key={h} style={S.th}>{h}</th>)}</tr></thead> <tbody> {filtered.map(p=>( <tr key={p.id} style={S.tr}> <td style={S.td}><code style={S.mono}>{p.code}</code></td> <td style={S.td}> <div style={{fontWeight:600,color:"#111827",fontSize:13}}>{p.name}</div> <div style={{fontSize:11,color:"#9ca3af",marginTop:1}}>{p.description}</div> </td> <td style={S.td}><span style={S.catTag}>{cats.find(c=>c.id===p.catId)?.icon} {catName(p.catId)}</span></td> <td style={S.td}><span style={{fontSize:13,color:"#374151"}}>{brandName(p.brandId)}</span></td> <td style={S.td}><StockBadge n={p.stock} alert={p.lowStockAlert}/></td> <td style={S.td}> <div style={{fontWeight:700,color:"#059669",fontSize:14}}>{fmtCLP(p.salePrice)}</div> {admin && <div style={{fontSize:11,color:"#9ca3af"}}>costo {fmtCLP(p.costPrice)} · mín {fmtCLP(p.minPrice)}</div>} </td> <td style={S.td}> <div style={{display:"flex",gap:6}}> {admin&&<Btn onClick={()=>openEdit(p)}>Editar</Btn>} {p.isPhone&&admin&&<Btn color="#1d4ed8" onClick={()=>openImei(p)}>IMEIs</Btn>} {admin&&<Btn color="#dc2626" onClick={()=>del(p.id)}>✕</Btn>} </div> </td> </tr>


=== PAGE 16 ===
))} {filtered.length===0&&<tr><td colSpan={7} style={{...S.td,textAlign:"center",color:"#9ca3af",padding:32}}>Sin resultados</td></tr>} </tbody> </table> </div> </>} {/* ── Tab Solicitudes (Admin) ── */} {tab==="solicitudes"&&admin&&( <div style={{display:"flex",flexDirection:"column",gap:12}}> {pendingRequests.length===0&&pendingCreditReqs.length===0&&( <div style={{textAlign:"center",padding:"40px 20px",background:"#fff",borderRadius:12,border:"1px solid #e5e7eb"}}> <div style={{fontSize:36}}> </div> <p style={{color:"#9ca3af",marginTop:10,fontSize:13}}>No hay solicitudes pendientes</p> </div> )} {pendingRequests.map(req=>{ const cat=cats.find(c=>c.id===req.product.catId); const brand=brands.find(b=>b.id===req.product.brandId); return ( <div key={req.id} style={{background:"#fff",border:"2px solid #fbbf24",borderRadius:14,padding:"16px 18px"}}> <div style={{display:"flex",alignItems:"flex-start",justifyContent:"space-between",gap:12,flexWrap:"wrap"}}> <div style={{flex:1}}> <div style={{display:"flex",alignItems:"center",gap:8,marginBottom:6}}> <span style={{background:"#fef3c7",color:"#92400e",fontSize:11,fontWeight:700,padding:"2px 8px",borderRadius:20}}> Pendiente</span> <span style={{fontSize:11,color:"#9ca3af"}}>{new Date(req.createdAt).toLocaleString("es-CL",{day:"2-digit",month:"2-digit",year:"numeric",hour:"2-digit",minute:"2-digit"})}</span> </div> <div style={{fontWeight:700,fontSize:15,color:"#111827"}}>{req.product.name}</div> <div style={{fontSize:12,color:"#6b7280",marginTop:2}}>{req.product.description}</div> <div style={{display:"flex",gap:10,marginTop:8,flexWrap:"wrap"}}> <span style={S.catTag}>{cat?.icon} {cat?.name||"Sin categoría"}</span> <span style={{background:"#f3f4f6",color:"#374151",fontSize:11,fontWeight:600,padding:"3px 8px",borderRadius:20}}>{brand?.name||"Sin marca"}</span> {req.product.isPhone&&<span style={{background:"#ede9fe",color:"#7c3aed",fontSize:11,fontWeight:600,padding:"3px 8px",borderRadius:20}}> Teléfono</span>} </div> <div style={{display:"flex",gap:14,marginTop:8,flexWrap:"wrap"}}> <span style={{fontSize:12,color:"#6b7280"}}>Precio venta: <strong style={{color:"#059669"}}>{fmtCLP(req.product.salePrice)}</strong></span> {req.product.minPrice>0&&<span style={{fontSize:12,color:"#6b7280"}}>Precio mínimo: <strong>{fmtCLP(req.product.minPrice)}</strong></span>} <span style={{fontSize:12,color:"#6b7280"}}>Stock: <strong>{req.product.stock}</strong></span> </div> <div style={{marginTop:6,fontSize:12,color:"#6b7280"}}>Solicitado por: <strong>{req.requestedByName}</strong></div> </div> <div style={{display:"flex",gap:8,flexShrink:0}}> <button style={{...S.btnPrimary,background:"#059669"}} onClick={()=>openApprove(req)}>✓ Aprobar</button> <button style={{...S.btnGhost,color:"#dc2626",borderColor:"#fca5a5"}} onClick={()=>rejectRequest(req.id)}>✕ Rechazar</button> </div> </div> </div>


=== PAGE 17 ===
); })} {/* ── Solicitudes de venta a crédito pendientes ── */} {pendingCreditReqs.length>0&&( <div style={{marginTop:pendingRequests.length>0?16:0}}> <div style={{fontSize:11,fontWeight:700,color:"#7c3aed",textTransform:"uppercase",letterSpacing:".06em",marginBottom:10}}> Ventas a crédito pendientes ({pendingCreditReqs.length}) </div> {pendingCreditReqs.map(cr=>( <div key={cr.id} style={{background:"#fff",border:"2px solid #a78bfa",borderRadius:14,padding:"16px 18px",marginBottom:10}}> <div style={{display:"flex",alignItems:"flex-start",justifyContent:"space-between",gap:12,flexWrap:"wrap"}}> <div style={{flex:1}}> <div style={{display:"flex",alignItems:"center",gap:8,marginBottom:6}}> <span style={{background:"#f5f3ff",color:"#6d28d9",fontSize:11,fontWeight:700,padding:"2px 8px",borderRadius:20}}> Pendiente crédito</span> <span style={{fontSize:11,color:"#9ca3af"}}>{new Date(cr.createdAt).toLocaleString("es-CL",{day:"2-digit",month:"2-digit",year:"numeric",hour:"2-digit",minute:"2-digit"})}</span> </div> <div style={{fontWeight:700,fontSize:15,color:"#111827"}}>{cr.productName}</div> <div style={{fontSize:12,color:"#6b7280",marginTop:2}}>{cr.productDesc}</div> {cr.imei&&<div style={{marginTop:4}}><code style={{...S.mono,fontSize:11,color:"#7c3aed"}}>IMEI: {cr.imei}</code></div>} <div style={{display:"flex",gap:12,marginTop:8,flexWrap:"wrap",fontSize:12,color:"#6b7280"}}> <span>Vendedor: <strong>{cr.requestedByName}</strong></span> <span>Precio venta: <strong style={{color:"#059669"}}>{fmtCLP(cr.salePrice)}</strong></span> <span>Entrada dada: <strong style={{color:"#2563eb"}}>{fmtCLP(cr.entradaDada)}</strong></span> <span>Retención {cr.retencion}%: <strong style={{color:"#dc2626"}}>-{fmtCLP(cr.retentionAmount)}</strong></span> {cr.useWeeks&&<span>{cr.semanasPerdonadas} sem. × {fmtCLP(cr.valorSemana)}</span>} </div> <div style={{marginTop:8,display:"flex",gap:16,flexWrap:"wrap"}}> <span style={{fontWeight:800,fontSize:14,color:"#7c3aed"}}>Total: {fmtCLP(cr.adminReceives)}</span> <span style={{fontWeight:700,fontSize:13,color:"#059669"}}>Ganancia neta: {fmtCLP(cr.adminReceives-(cr.costPrice||0))}</span> </div> </div> <div style={{display:"flex",gap:6,flexShrink:0}}> <button style={{...S.btnPrimary,background:"#059669",fontSize:12,padding:"6px 14px"}} onClick={()=>approveCreditInv(cr)}>✓ Aprobar</button> <button style={{...S.btnGhost,color:"#dc2626",fontSize:12,padding:"6px 14px"}} onClick={()=>denyCreditInv(cr)}>✕ Denegar</button> </div> </div> </div> ))} </div> )} {/* ── Solicitudes de venta con tarjeta pendientes ── */} {pendingCardReqs.length>0&&( <div style={{marginTop:pendingCreditReqs.length>0||pendingRequests.length>0?16:0}}> <div style={{fontSize:11,fontWeight:700,color:"#1d4ed8",textTransform:"uppercase",letterSpacing:".06em",marginBottom:10}}> Ventas con tarjeta pendientes ({pendingCardReqs.length})


=== PAGE 18 ===
</div> {pendingCardReqs.map(cd=>( <div key={cd.id} style={{background:"#fff",border:"2px solid #93c5fd",borderRadius:14,padding:"16px 18px",marginBottom:10}}> <div style={{display:"flex",alignItems:"flex-start",justifyContent:"space-between",gap:12,flexWrap:"wrap"}}> <div style={{flex:1}}> <div style={{display:"flex",alignItems:"center",gap:8,marginBottom:6}}> <span style={{background:"#eff6ff",color:"#1d4ed8",fontSize:11,fontWeight:700,padding:"2px 8px",borderRadius:20}}> Pendiente tarjeta</span> <span style={{fontSize:11,color:"#9ca3af"}}>{new Date(cd.createdAt).toLocaleString("es-CL",{day:"2-digit",month:"2-digit",year:"numeric",hour:"2-digit",minute:"2-digit"})}</span> </div> <div style={{fontWeight:700,fontSize:15,color:"#111827"}}>{cd.productName}</div> <div style={{fontSize:12,color:"#6b7280",marginTop:2}}>{cd.productDesc}</div> {cd.imei&&<div style={{marginTop:4}}><code style={{...S.mono,fontSize:11,color:"#2563eb"}}>IMEI: {cd.imei}</code></div>} <div style={{display:"flex",gap:12,marginTop:8,flexWrap:"wrap",fontSize:12,color:"#6b7280"}}> <span>Vendedor: <strong>{cd.requestedByName}</strong></span> <span>Valor producto: <strong style={{color:"#059669"}}>{fmtCLP(cd.salePrice)}</strong></span> <span>Con recargo ({cd.pct}%): <strong style={{color:"#2563eb"}}>{fmtCLP(cd.finalPrice)}</strong></span> {cd.useCuotas&&<span>Diferido: <strong>{cd.meses} meses</strong>{cd.valorMes?<> · <strong style={{color:"#1d4ed8"}}>{fmtCLP(cd.valorMes)}/mes</strong></>:""}</span>} </div> <div style={{marginTop:8,display:"flex",gap:16,flexWrap:"wrap"}}> <span style={{fontWeight:800,fontSize:14,color:"#1d4ed8"}}>Total: {fmtCLP(cd.salePrice)}</span> <span style={{fontWeight:700,fontSize:13,color:"#059669"}}>Ganancia: {fmtCLP(cd.salePrice-(cd.costPrice||0))}</span> </div> </div> <div style={{display:"flex",gap:6,flexShrink:0}}> <button style={{...S.btnPrimary,background:"#059669",fontSize:12,padding:"6px 14px"}} onClick={()=>approveCardInv(cd)}>✓ Aprobar</button> <button style={{...S.btnGhost,color:"#dc2626",fontSize:12,padding:"6px 14px"}} onClick={()=>denyCardInv(cd)}>✕ Denegar</button> </div> </div> </div> ))} </div> )} {/* Historial de solicitudes de productos procesadas */} {requests.filter(r=>r.status!=="pending").length>0&&( <div style={{marginTop:8}}> <div style={{fontSize:11,fontWeight:700,color:"#9ca3af",textTransform:"uppercase",letterSpacing:".06em",marginBottom:10}}>Historial</div> {requests.filter(r=>r.status!=="pending").slice().sort((a,b)=>b.createdAt-a.createdAt).map(req=>( <div key={req.id} style={{display:"flex",alignItems:"center",gap:10,padding:"10px 14px",background:"#f9fafb",border:"1px solid #e5e7eb",borderRadius:10,marginBottom:6}}> <span style={{fontSize:12,fontWeight:700,padding:"2px 8px",borderRadius:20,...(req.status==="approved"?{background:"#dcfce7",color:"#166534"}:{background:"#fee2e2",color:"#991b1b"})}}> {req.status==="approved"?"✓ Aprobado":"✕ Rechazado"} </span> <span style={{flex:1,fontSize:13,color:"#374151",fontWeight:500}}>{req.product.name}</span> <span style={{fontSize:11,color:"#9ca3af"}}>{req.requestedByName}</span> </div> ))} </div>


=== PAGE 19 ===
)} </div> )} {/* ── Tab Mis Solicitudes (Vendedor) ── */} {tab==="mis solicitudes"&&!admin&&( <div style={{display:"flex",flexDirection:"column",gap:10}}> {requests.filter(r=>r.requestedBy===session.id).length===0&&( <div style={{textAlign:"center",padding:"40px 20px",background:"#fff",borderRadius:12,border:"1px solid #e5e7eb"}}> <div style={{fontSize:36}}> </div> <p style={{color:"#9ca3af",marginTop:10,fontSize:13}}>No has enviado solicitudes aún</p> </div> )} {requests.filter(r=>r.requestedBy===session.id).slice().sort((a,b)=>b.createdAt-a.createdAt).map(req=>{ const cat=cats.find(c=>c.id===req.product.catId); const statusMap={pending:{bg:"#fef3c7",cl:"#92400e",label:" Pendiente"},approved:{bg:"#dcfce7",cl:"#166534",label:"✓ Aprobado"},rejected:{bg:"#fee2e2",cl:"#991b1b",label:"✕ Rechazado"}}; const st=statusMap[req.status]; return ( <div key={req.id} style={{background:"#fff",border:"1px solid #e5e7eb",borderRadius:12,padding:"14px 16px"}}> <div style={{display:"flex",justifyContent:"space-between",alignItems:"flex-start",gap:10,flexWrap:"wrap"}}> <div style={{flex:1}}> <div style={{display:"flex",alignItems:"center",gap:8,marginBottom:4}}> <span style={{background:st.bg,color:st.cl,fontSize:11,fontWeight:700,padding:"2px 8px",borderRadius:20}}>{st.label}</span> <span style={{fontSize:11,color:"#9ca3af"}}>{new Date(req.createdAt).toLocaleDateString("es-CL")}</span> </div> <div style={{fontWeight:700,fontSize:14,color:"#111827"}}>{req.product.name}</div> <div style={{fontSize:12,color:"#6b7280",marginTop:2}}>{req.product.description}</div> <div style={{display:"flex",gap:10,marginTop:6,flexWrap:"wrap"}}> <span style={S.catTag}>{cat?.icon} {cat?.name}</span> <span style={{fontSize:12,color:"#6b7280"}}>Precio venta: <strong style={{color:"#059669"}}>{fmtCLP(req.product.salePrice)}</strong></span> </div> </div> </div> </div> ); })} </div> )} {tab==="categorías"&&<CatsTab cats={cats} setCats={setCats} brands={brands} products={products} notify={notify} admin={admin}/>} {tab==="marcas"&&<BrandsTab brands={brands} setBrands={setBrands} cats={cats} products={products} notify={notify} admin={admin}/>} {/* ── Modal: Formulario Admin (nuevo/editar) ── */} {modal==="form"&&( <Modal title={form.id?"Editar Producto":"Nuevo Producto"} onClose={()=>setModal(null)}> <div style={S.fgrid}> <Fld label="Código (autogenerado)"><input style={{...S.input,background:"#f9fafb",color:"#9ca3af"}} value={form.code} readOnly/></Fld>


=== PAGE 20 ===
<Fld label="Categoría *"> <select style={S.select} value={form.catId} onChange={e=>{ const cat=cats.find(c=>c.id===e.target.value); setForm({...form,catId:e.target.value,brandId:"",code:genCode(cat?.name||"","")}); }}> <option value="">Seleccionar…</option>{cats.map(c=><option key={c.id} value={c.id}>{c.icon} {c.name}</option>)} </select> </Fld> <Fld label="Marca *"> <select style={S.select} value={form.brandId} onChange={e=>{ const cat=cats.find(c=>c.id===form.catId); const brand=brands.find(b=>b.id===e.target.value); setForm({...form,brandId:e.target.value,code:genCode(cat?.name||"",brand?.name||"")}); }}> <option value="">Seleccionar…</option>{brands.filter(b=>b.catId===form.catId).map(b=><option key={b.id} value={b.id}>{b.name}</option>)} </select> </Fld> <Fld label="Nombre del producto *"><input style={S.input} value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></Fld> <Fld label="Descripción" full><textarea style={{...S.input,height:60,resize:"vertical"}} value={form.description} onChange={e=>setForm({...form,description:e.target.value})}/></Fld> <Fld label="Precio Adquisición"><input style={S.input} type="number" value={form.costPrice} onChange={e=>setForm({...form,costPrice:+e.target.value})}/></Fld> <Fld label="Precio de Venta"><input style={S.input} type="number" value={form.salePrice} onChange={e=>setForm({...form,salePrice:+e.target.value})}/></Fld> <Fld label="Precio Mínimo (con descuento)"><input style={S.input} type="number" value={form.minPrice} onChange={e=>setForm({...form,minPrice:+e.target.value})}/></Fld> <Fld label="Stock actual"><input style={S.input} type="number" value={form.stock} onChange={e=>setForm({...form,stock:+e.target.value})}/></Fld> <Fld label="Alerta stock bajo (uds.)"><input style={S.input} type="number" value={form.lowStockAlert} onChange={e=>setForm({...form,lowStockAlert:+e.target.value})}/></Fld> <Fld full> <label style={{display:"flex",alignItems:"center",gap:8,cursor:"pointer",fontSize:13,color:"#374151"}}> <input type="checkbox" checked={form.isPhone} onChange={e=>setForm({...form,isPhone:e.target.checked})} style={{width:16,height:16,accentColor:"#2563eb"}}/> Es teléfono — habilitar gestión de IMEIs </label> </Fld> </div> <div style={{display:"flex",gap:8,marginTop:18}}> <button style={S.btnPrimary} onClick={saveAdmin}>Guardar</button> <button style={S.btnGhost} onClick={()=>setModal(null)}>Cancelar</button> </div> </Modal> )} {/* ── Modal: Solicitud Vendedor (sin precio adquisición) ── */} {modal==="request"&&( <Modal title="Solicitar Nuevo Producto" onClose={()=>setModal(null)}> <div style={{background:"#eff6ff",border:"1px solid #bfdbfe",borderRadius:10,padding:"10px 14px",marginBottom:16,fontSize:12,color:"#1d4ed8"}}> Tu solicitud será enviada al administrador para su aprobación antes de agregar el producto al inventario. </div> <div style={S.fgrid}> <Fld label="Categoría *"> <select style={S.select} value={form.catId} onChange={e=>{


=== PAGE 21 ===
const cat=cats.find(c=>c.id===e.target.value); setForm({...form,catId:e.target.value,brandId:"",code:genCode(cat?.name||"","")}); }}> <option value="">Seleccionar…</option>{cats.map(c=><option key={c.id} value={c.id}>{c.icon} {c.name}</option>)} </select> </Fld> <Fld label="Marca *"> <select style={S.select} value={form.brandId} onChange={e=>{ const cat=cats.find(c=>c.id===form.catId); const brand=brands.find(b=>b.id===e.target.value); setForm({...form,brandId:e.target.value,code:genCode(cat?.name||"",brand?.name||"")}); }}> <option value="">Seleccionar…</option>{brands.filter(b=>b.catId===form.catId).map(b=><option key={b.id} value={b.id}>{b.name}</option>)} </select> </Fld> <Fld label="Nombre del producto *" full><input style={S.input} value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></Fld> <Fld label="Descripción" full><textarea style={{...S.input,height:60,resize:"vertical"}} value={form.description} onChange={e=>setForm({...form,description:e.target.value})}/></Fld> <Fld label="Precio de Venta sugerido"><input style={S.input} type="number" value={form.salePrice} onChange={e=>setForm({...form,salePrice:+e.target.value})}/></Fld> <Fld label="Precio Mínimo sugerido"><input style={S.input} type="number" value={form.minPrice} onChange={e=>setForm({...form,minPrice:+e.target.value})}/></Fld> <Fld label="Stock inicial"><input style={S.input} type="number" value={form.stock} onChange={e=>setForm({...form,stock:+e.target.value})}/></Fld> <Fld label="Alerta stock bajo"><input style={S.input} type="number" value={form.lowStockAlert} onChange={e=>setForm({...form,lowStockAlert:+e.target.value})}/></Fld> <Fld full> <label style={{display:"flex",alignItems:"center",gap:8,cursor:"pointer",fontSize:13,color:"#374151"}}> <input type="checkbox" checked={form.isPhone} onChange={e=>setForm({...form,isPhone:e.target.checked})} style={{width:16,height:16,accentColor:"#2563eb"}}/> Es teléfono — habilitar gestión de IMEIs </label> </Fld> </div> <div style={{display:"flex",gap:8,marginTop:18}}> <button style={S.btnPrimary} onClick={sendRequest}> Enviar Solicitud</button> <button style={S.btnGhost} onClick={()=>setModal(null)}>Cancelar</button> </div> </Modal> )} {/* ── Modal: Aprobar solicitud (Admin completa precio adquisición) ── */} {modal==="approve"&&( <Modal title="Aprobar Solicitud de Producto" onClose={()=>setModal(null)}> <div style={{background:"#f0fdf4",border:"1px solid #86efac",borderRadius:10,padding:"12px 14px",marginBottom:16}}> <div style={{fontSize:12,fontWeight:700,color:"#166534",marginBottom:4}}> Información del producto solicitado</div> <div style={{fontWeight:700,fontSize:15,color:"#111827"}}>{approveForm.name}</div> {approveForm.description&&<div style={{fontSize:12,color:"#6b7280",marginTop:2}}>{approveForm.description}</div>} <div style={{display:"flex",gap:12,marginTop:8,flexWrap:"wrap",fontSize:12,color:"#6b7280"}}> <span>Categoría: <strong>{cats.find(c=>c.id===approveForm.catId)?.name}</strong></span> <span>Marca: <strong>{brands.find(b=>b.id===approveForm.brandId)?.name}</strong></span> <span>Precio venta sugerido: <strong style={{color:"#059669"}}>{fmtCLP(approveForm.salePrice)}</strong></span> <span>Stock: <strong>{approveForm.stock}</strong></span>


=== PAGE 22 ===
</div> </div> <div style={{background:"#fefce8",border:"1px solid #fde047",borderRadius:10,padding:"12px 14px",marginBottom:16}}> <label style={{...S.label,color:"#854d0e"}}>Precio de Adquisición *</label> <input style={S.input} type="number" placeholder="Ingresa el precio de costo…" value={approveForm.costPrice||""} onChange={e=>setApproveForm({...approveForm,costPrice:e.target.value})} autoFocus/> <div style={{fontSize:11,color:"#92400e",marginTop:6}}>Este dato solo es visible para los administradores.</div> </div> <div style={{display:"flex",gap:8}}> <button style={{...S.btnPrimary,background:"#059669",flex:1}} onClick={approveRequest}>✓ Aprobar y Agregar al Inventario</button> <button style={{...S.btnGhost,color:"#dc2626",borderColor:"#fca5a5"}} onClick={()=>rejectRequest(approveForm.reqId)}>✕ Rechazar</button> </div> </Modal> )} {modal==="imei"&&( <Modal title={`IMEIs — ${form.name}`} onClose={()=>setModal(null)}> <div style={{display:"flex",gap:8,marginBottom:14}}> <input style={{...S.input,flex:1,fontFamily:"'JetBrains Mono',monospace"}} placeholder="IMEI (solo números)" value={form.newImei||""} onChange={e=>setForm({...form,newImei:e.target.value.replace(/\D/g,"")})} onKeyDown={e=>e.key==="Enter"&&addImei()} maxLength={15} inputMode="numeric"/> <button style={S.btnPrimary} onClick={addImei}>Agregar</button> </div> <div style={{display:"flex",flexDirection:"column",gap:6,maxHeight:280,overflowY:"auto"}}> {form.imeis.map((imei,i)=>( <div key={i} style={{display:"flex",alignItems:"center",gap:8,background:"#f9fafb",border:"1px solid #e5e7eb",borderRadius:8,padding:"8px 12px"}}> <code style={{flex:1,fontSize:13,color:"#1d4ed8",fontFamily:"'JetBrains Mono',monospace"}}>{imei}</code> <button style={{...S.btnGhost,padding:"2px 8px",fontSize:12,color:"#dc2626"}} onClick={()=>removeImei(imei)}>Eliminar</button> </div> ))} {!form.imeis.length&&<p style={S.empty}>Sin IMEIs registrados</p>} </div> <div style={{marginTop:10,fontSize:12,color:"#1d4ed8",background:"#eff6ff",padding:"8px 12px",borderRadius:8}}> Stock automático: <strong>{form.imeis.length} unidades</strong> </div> </Modal> )} </Page> ); } function CatsTab({cats,setCats,brands,products,notify,admin}) { const [name,setName]=useState(""); const [icon,setIcon]=useState(" "); const delCat = (c) => { const hasBrands = brands.some(b=>b.catId===c.id); const hasProducts = products.some(p=>p.catId===c.id); if (hasBrands||hasProducts) { notify("No se puede eliminar: tiene marcas o productos asociados","err"); return; }


=== PAGE 23 ===
setCats(cats.filter(x=>x.id!==c.id)); notify("Categoría eliminada"); }; return ( <div> {admin&&( <div style={{display:"flex",gap:8,marginBottom:20,alignItems:"flex-end",flexWrap:"wrap"}}> <div><label style={S.label}>Emoji</label><input style={{...S.input,width:70,textAlign:"center",fontSize:20}} value={icon} onChange={e=>setIcon(e.target.value)}/></div> <div style={{flex:1,minWidth:160}}><label style={S.label}>Nombre</label><input style={S.input} value={name} onChange={e=>setName(e.target.value)} onKeyDown={e=>e.key==="Enter"&&(setCats([...cats,{id:"cat"+Date.now(),name,icon}]),setName(""),notify("Categoría creada"))}/></div> <button style={{...S.btnPrimary,height:40,flexShrink:0}} onClick={()=>{if(name){setCats([...cats,{id:"cat"+Date.now(),name,icon}]);setName("");notify("Categoría creada");}}}>Agregar</button> </div> )} <div style={{display:"grid",gridTemplateColumns:"repeat(auto-fill,minmax(150px,1fr))",gap:12}}> {cats.map(c=>( <div key={c.id} style={{background:"#f9fafb",border:"1px solid #e5e7eb",borderRadius:12,padding:"14px 12px",textAlign:"center",position:"relative"}}> {admin&&( <button onClick={()=>delCat(c)} title="Eliminar categoría" style={{position:"absolute",top:6,right:6,background:"#fee2e2",border:"none",borderRadius:6,width:22,height:22,cursor:"pointer",fontSize:12,color:"#dc2626",display:"flex",alignItems:"center",justifyContent:"center",lineHeight:1}}> ✕ </button> )} <div style={{fontSize:32}}>{c.icon}</div> <div style={{fontWeight:600,color:"#111827",marginTop:8,fontSize:13}}>{c.name}</div> <div style={{fontSize:10,color:"#9ca3af",marginTop:3}}> {products.filter(p=>p.catId===c.id).length} productos </div> </div> ))} </div> </div> ); } function BrandsTab({brands,setBrands,cats,products,notify,admin}) { const [catId,setCatId]=useState(""); const [name,setName]=useState(""); const add=()=>{ if(!name||!catId)return; setBrands([...brands,{id:"br"+Date.now(),catId,name}]); setName(""); notify("Marca creada"); }; const delBrand = (b) => { if (products.some(p=>p.brandId===b.id)) { notify("No se puede eliminar: hay productos con esta marca","err"); return; } setBrands(brands.filter(x=>x.id!==b.id)); notify("Marca eliminada"); }; return ( <div> {admin&&( <div style={{display:"flex",gap:8,marginBottom:20,alignItems:"flex-end",flexWrap:"wrap"}}> <div style={{width:200}}><label style={S.label}>Categoría</label><select style={S.select} value={catId} onChange={e=>setCatId(e.target.value)}><option value="">Seleccionar…</option>{cats.map(c=><option key={c.id} value={c.id}>{c.icon} {c.name}</option>)}</select></div> <div style={{flex:1,minWidth:160}}><label style={S.label}>Marca</label><input style={S.input} value={name} onChange={e=>setName(e.target.value)} onKeyDown={e=>e.key==="Enter"&&add()}/></div> <button style={{...S.btnPrimary,height:40}} onClick={add}>Agregar</button> </div>


=== PAGE 24 ===
)} {cats.map(c=>{ const cb=brands.filter(b=>b.catId===c.id); if(!cb.length)return null; return ( <div key={c.id} style={{marginBottom:20}}> <div style={{fontSize:11,fontWeight:700,color:"#9ca3af",textTransform:"uppercase",letterSpacing:".06em",marginBottom:8}}>{c.icon} {c.name}</div> <div style={{display:"flex",flexWrap:"wrap",gap:8}}> {cb.map(b=>( <div key={b.id} style={{display:"flex",alignItems:"center",gap:0,background:"#f3f4f6",borderRadius:20,overflow:"hidden",border:"1px solid #e5e7eb"}}> <span style={{padding:"5px 12px",fontSize:12,fontWeight:500,color:"#374151"}}>{b.name}</span> {admin&&( <button onClick={()=>delBrand(b)} title="Eliminar marca" style={{background:"transparent",border:"none",borderLeft:"1px solid #e5e7eb",cursor:"pointer",padding:"5px 8px",fontSize:11,color:"#9ca3af",lineHeight:1,transition:"all .15s"}} onMouseEnter={e=>{ e.target.style.background="#fee2e2"; e.target.style.color="#dc2626"; }} onMouseLeave={e=>{ e.target.style.background="transparent"; e.target.style.color="#9ca3af"; }}> ✕ </button> )} </div> ))} </div> </div> );})} </div> ); } /* ─── VENTAS ─────────────────────────────────────────────────────────────── */ function Sales({ products, setProducts, sales, setSales, expenses, setExpenses, creditSales, setCreditSales, cardSales, setCardSales, notifs, setNotifs, users, cats, session, notify }) { const admin = isAdmin(session); const [search,setSearch]=useState(""); const [selected,setSelected]=useState(null); const [salePrice,setSalePrice]=useState(""); const [imeiUsed,setImeiUsed]=useState(""); const [saleDate,setSaleDate]=useState(today()); const [mode,setMode]=useState("venta"); const [payMethod,setPayMethod]=useState("efectivo"); // "efectivo" | "transferencia" const [expDesc,setExpDesc]=useState(""); const [expAmount,setExpAmount]=useState(""); // ── Crédito state ────────────────────────────────────────────────────── const EMPTY_CRED = {salePrice:"",entradaPedida:"",entradaDada:"",retencion:15, semanasPerdonadas:"",valorSemana:"",useWeeks:false,imei:""}; const [credSel,setCredSel]=useState(null); const [cf,setCf]=useState(EMPTY_CRED); const [pendingView,setPendingView]=useState(null); // ── Tarjeta state ────────────────────────────────────────────────────── const EMPTY_CARD = {salePrice:"", pct:20, finalPrice:"", useCuotas:false, meses:""}; const [cardSel,setCardSel]=useState(null);


=== PAGE 25 ===
const [cardF,setCardF]=useState(EMPTY_CARD); const calcCardFinal = (sp, pct) => Math.round((+sp||0) * (1 + (+pct||0)/100)); const pendingCardSales = cardSales.filter(c=>c.status==="pending"); // credit detail modal for admin // Phone products: any product with isPhone flag const phoneProducts = products.filter(p=>p.isPhone && p.stock>0); const calcAdminGets = (form, p) => { if(!p) return 0; const sp=+form.salePrice||0, ep=+form.entradaPedida||0, ed=+form.entradaDada||0; const sw=form.useWeeks?(+form.semanasPerdonadas||0)*(+form.valorSemana||0):0; const ret=sp*((+form.retencion||0)/100); return sp - ep + ed - sw - ret; }; // ── Ventas normales ──────────────────────────────────────────────────── const todaySales = admin ? sales.filter(s=>s.date===saleDate) : sales.filter(s=>s.date===saleDate&&s.userId===session.id); const todayExpenses = admin ? expenses.filter(e=>e.date===saleDate) : expenses.filter(e=>e.date===saleDate&&e.userId===session.id); const totalRevenue=todaySales.reduce((a,s)=>a+s.salePrice,0); const totalCosts=todaySales.reduce((a,s)=>a+(s.costPrice||0),0); const totalExpenses=todayExpenses.reduce((a,e)=>a+e.amount,0); const netDay=totalRevenue-totalCosts-totalExpenses; // ganancia real: ingresos - costo - gastos const available=products.filter(p=>p.stock>0&&(p.name.toLowerCase().includes(search.toLowerCase())||p.code.includes(search))); const confirmSale=()=>{ if(!selected)return; const price=+salePrice; if(selected.isPhone&&!imeiUsed){notify("Selecciona el IMEI a vender","err");return;} const cat=cats.find(c=>c.id===selected.catId); if(selected.minPrice&&price<selected.minPrice) notify(` Precio por debajo del mínimo (${fmtCLP(selected.minPrice)})`,"warn"); else if(price>selected.salePrice) notify(` Precio por encima del precio de venta (${fmtCLP(selected.salePrice)})`,"warn"); setSales([...sales,{id:"s"+Date.now(),productId:selected.id,productName:selected.name, catName:cat?.name||"",userId:session.id,userName:session.name,date:saleDate, salePrice:price,costPrice:selected.costPrice,imei:imeiUsed||null,payMethod}]); setProducts(products.map(p=>{ if(p.id!==selected.id)return p; const imeis=p.isPhone&&imeiUsed?p.imeis.filter(i=>i!==imeiUsed):p.imeis; return {...p,stock:p.stock-1,imeis}; })); if(selected.stock-1<=selected.lowStockAlert) notify(` Stock bajo: "${selected.name}"`,"warn"); else notify(`Venta registrada — ${fmtCLP(price)}`); setSelected(null); setSearch(""); setSalePrice(""); setImeiUsed(""); setPayMethod("efectivo"); };


=== PAGE 26 ===
const confirmExpense=()=>{ if(!expDesc.trim()||!expAmount||+expAmount<=0){notify("Ingresa descripción y monto válido","err");return;} setExpenses([...expenses,{id:"e"+Date.now(),description:expDesc.trim(),amount:+expAmount, date:saleDate,userId:session.id,userName:session.name}]); notify(`Gasto registrado — ${fmtCLP(+expAmount)}`); setExpDesc(""); setExpAmount(""); }; const deleteExpense=(id)=>{ setExpenses(expenses.filter(e=>e.id!==id)); notify("Gasto eliminado"); }; // ── Enviar solicitud crédito ─────────────────────────────────────────── const submitCredit=()=>{ if(!credSel){notify("Selecciona un teléfono","err");return;} if(!cf.salePrice||!cf.entradaPedida||!cf.entradaDada){notify("Completa los valores requeridos","err");return;} if(credSel.isPhone&&!cf.imei){notify("Selecciona el IMEI a vender","err");return;} const adminGets=calcAdminGets(cf,credSel); const retAmt=(+cf.salePrice)*((+cf.retencion)/100); const cr={ id:"cr"+Date.now(), status:"pending", createdAt:Date.now(), requestedBy:session.id, requestedByName:session.name, productId:credSel.id, productName:credSel.name, productDesc:credSel.description||"", imei:cf.imei, salePrice:+cf.salePrice, entradaPedida:+cf.entradaPedida, entradaDada:+cf.entradaDada, retencion:+cf.retencion, useWeeks:cf.useWeeks, semanasPerdonadas:cf.useWeeks?+cf.semanasPerdonadas:0, valorSemana:cf.useWeeks?+cf.valorSemana:0, adminReceives:adminGets, retentionAmount:retAmt, costPrice:credSel.costPrice||0, }; // Descontar stock AHORA setProducts(products.map(p=>{ if(p.id!==credSel.id)return p; const imeis=p.isPhone&&cf.imei?p.imeis.filter(i=>i!==cf.imei):p.imeis; return {...p,stock:p.stock-1,imeis}; })); setCreditSales(prev=>[...prev,cr]); // Notificar admins const adminNotif = (uid) => ({ id:"n"+Date.now()+uid, userId:uid, read:false, type:"credit_pending", msg:`Nueva venta a crédito: ${credSel.name} · Recibirás ${fmtCLP(adminGets)}`, createdAt:Date.now() }); const newNotifs=[...notifs, ...users.filter(u=>u.role==="admin").map(u=>adminNotif(u.id))]; // Si hay semanas perdonadas → recordatorio a todos un día antes del primer pago (día 6) if(cf.useWeeks&&+cf.semanasPerdonadas>0&&+cf.valorSemana>0){ const payTs=Date.now()+(6*24*60*60*1000); // en 6 días (recordatorio para día 7) const payDate=new Date(payTs).toISOString().split("T")[0]; const remMsg=` Recordatorio: mañana vence 1er pago de ${fmtCLP(+cf.valorSemana)}/semana — ${credSel.name}`; const involved=[session.id,...users.filter(u=>u.role==="admin").map(u=>u.id)];


=== PAGE 27 ===
involved.forEach(uid=>{ newNotifs.push({id:"n"+Date.now()+uid+"w",userId:uid,read:false, type:"week_reminder",msg:remMsg,dueDate:payDate,createdAt:Date.now()}); }); } setNotifs(newNotifs); notify("✓ Solicitud de crédito enviada al administrador"); setCredSel(null); setCf(EMPTY_CRED); }; // ── Admin: aprobar crédito ───────────────────────────────────────────── const approveCredit=(cr)=>{ const approvedDate=today(); const p=products.find(x=>x.id===cr.productId); const cat=cats.find(c=>c.id===p?.catId); setSales(prev=>[...prev,{ id:"s"+Date.now(), productId:cr.productId, productName:cr.productName, catName:cat?.name||"Teléfonos", userId:cr.requestedBy, userName:cr.requestedByName, date:approvedDate, salePrice:cr.adminReceives, // ingreso real del admin costPrice:cr.costPrice||0, // costo del producto (para ganancia neta) salePriceOriginal:cr.salePrice, // precio de venta original (referencia) imei:cr.imei||null, isCredit:true }]); setCreditSales(prev=>prev.map(c=>c.id===cr.id?{...c,status:"approved",approvedAt:approvedDate}:c)); setPendingView(null); notify("✓ Venta a crédito aprobada y registrada"); }; const denyCredit=(cr)=>{ // Devolver stock const p=products.find(x=>x.id===cr.productId); if(p){ setProducts(products.map(x=>{ if(x.id!==cr.productId)return x; const imeis=cr.imei?[...x.imeis,cr.imei]:x.imeis; return {...x,stock:x.stock+1,imeis}; })); } setCreditSales(prev=>prev.map(c=>c.id===cr.id?{...c,status:"denied"}:c)); setPendingView(null); notify("Venta a crédito denegada","warn"); }; // ── Enviar solicitud venta con tarjeta ──────────────────────────────── const submitCard = () => { if (!cardSel) { notify("Selecciona un producto","err"); return; }


=== PAGE 28 ===
if (!cardF.salePrice) { notify("Ingresa el valor del producto","err"); return; } const finalPrice = +cardF.finalPrice || calcCardFinal(cardF.salePrice, cardF.pct); const card = { id:"cd"+Date.now(), status:"pending", createdAt:Date.now(), requestedBy:session.id, requestedByName:session.name, productId:cardSel.id, productName:cardSel.name, productDesc:cardSel.description||"", imei:cardSel.isPhone?(cardF.imei||null):null, salePrice:+cardF.salePrice, pct:+cardF.pct, finalPrice, useCuotas:cardF.useCuotas, meses:cardF.useCuotas?+cardF.meses:0, valorMes:cardF.useCuotas&&cardF.meses?Math.ceil(finalPrice/(+cardF.meses)):0, costPrice:cardSel.costPrice||0, }; // Descontar stock inmediatamente setProducts(products.map(p => { if (p.id!==cardSel.id) return p; const imeis = p.isPhone&&card.imei ? p.imeis.filter(i=>i!==card.imei) : p.imeis; return {...p, stock:p.stock-1, imeis}; })); setCardSales(prev=>[...prev, card]); // Notificar admins users.filter(u=>u.role==="admin").forEach(u=>{ setNotifs(prev=>[...prev,{ id:"n"+Date.now()+u.id, userId:u.id, read:false, type:"card_pending", msg:`Nueva venta con tarjeta: ${cardSel.name} — ${session.name} · ${fmtCLP(+cardF.salePrice)}`, createdAt:Date.now() }]); }); notify("✓ Solicitud de venta con tarjeta enviada"); setCardSel(null); setCardF(EMPTY_CARD); }; const approveCard = (cd) => { const approvedDate = today(); const p = products.find(x=>x.id===cd.productId); const cat = cats.find(c=>c.id===p?.catId); setSales(prev=>[...prev,{ id:"s"+Date.now(), productId:cd.productId, productName:cd.productName, catName:cat?.name||"", userId:cd.requestedBy, userName:cd.requestedByName, date:approvedDate, salePrice:cd.salePrice, costPrice:cd.costPrice||0, imei:cd.imei||null, isCard:true, payMethod:"tarjeta", }]); setCardSales(prev=>prev.map(c=>c.id===cd.id?{...c,status:"approved",approvedAt:approvedDate}:c)); notify("✓ Venta con tarjeta aprobada y registrada"); }; const denyCard = (cd) => { setProducts(products.map(p=>{


=== PAGE 29 ===
if (p.id!==cd.productId) return p; const imeis = cd.imei ? [...p.imeis, cd.imei] : p.imeis; return {...p, stock:p.stock+1, imeis}; })); setCardSales(prev=>prev.map(c=>c.id===cd.id?{...c,status:"denied"}:c)); notify("Venta con tarjeta denegada","warn"); }; const deleteSale = (saleId) => { const s = sales.find(x=>x.id===saleId); if (!s) return; setProducts(products.map(p=>{ if(p.id!==s.productId) return p; const imeis = s.imei ? [...p.imeis, s.imei] : p.imeis; return {...p, stock:p.stock+1, imeis}; })); setSales(sales.filter(x=>x.id!==saleId)); notify("Venta eliminada"); }; const pendingCredits = creditSales.filter(c=>c.status==="pending"); const pageSub = admin ? `${todaySales.length} ventas · Ingresos ${fmtCLP(totalRevenue)} · Gastos ${fmtCLP(totalExpenses)} · Ganancias ${fmtCLP(netDay)}` : `${todaySales.length} mis ventas · ${fmtCLP(totalRevenue)} en ingresos`; return ( <Page title="Registro de Ventas" sub={pageSub} action={ <div style={{display:"flex",gap:8,alignItems:"center",flexWrap:"wrap"}}> {admin&&<input type="date" style={{...S.input,width:"auto"}} value={saleDate} onChange={e=>setSaleDate(e.target.value)}/>} </div> }> {/* ── Admin: panel de créditos pendientes ── */} {admin&&pendingCredits.length>0&&( <div style={{background:"#fffbeb",border:"2px solid #fbbf24",borderRadius:14,padding:"14px 18px",marginBottom:20}}> <div style={{fontWeight:700,fontSize:13,color:"#92400e",marginBottom:10}}> Solicitudes de crédito pendientes ({pendingCredits.length}) </div> <div style={{display:"flex",flexDirection:"column",gap:8}}> {pendingCredits.map(cr=>( <div key={cr.id} style={{background:"#fff",border:"1px solid #fde68a",borderRadius:10,padding:"12px 14px",display:"flex",gap:12,alignItems:"center",flexWrap:"wrap"}}> <div style={{flex:1,minWidth:200}}> <div style={{fontWeight:700,fontSize:13,color:"#111827"}}>{cr.productName}</div> <div style={{fontSize:11,color:"#9ca3af"}}>{cr.productDesc}</div> <div style={{fontSize:11,color:"#6b7280",marginTop:4}}>


=== PAGE 30 ===
{new Date(cr.createdAt).toLocaleString("es-CL",{day:"2-digit",month:"2-digit",year:"numeric",hour:"2-digit",minute:"2-digit"})} {" · "}{cr.requestedByName} {cr.imei&&<span> · <code style={S.mono}>{cr.imei}</code></span>} </div> <div style={{display:"flex",gap:12,marginTop:6,flexWrap:"wrap",fontSize:12}}> <span style={{color:"#6b7280"}}>Venta: <strong style={{color:"#059669"}}>{fmtCLP(cr.salePrice)}</strong></span> <span style={{color:"#6b7280"}}>Entrada pedida: <strong>{fmtCLP(cr.entradaPedida)}</strong></span> <span style={{color:"#6b7280"}}>Entrada dada: <strong style={{color:"#2563eb"}}>{fmtCLP(cr.entradaDada)}</strong></span> {cr.useWeeks&&<span style={{color:"#6b7280"}}>{cr.semanasPerdonadas} sem. × {fmtCLP(cr.valorSemana)}</span>} <span style={{color:"#6b7280"}}>Retención {cr.retencion}%: <strong style={{color:"#dc2626"}}>-{fmtCLP(cr.retentionAmount)}</strong></span> </div> <div style={{marginTop:6,fontWeight:800,fontSize:14,color:"#7c3aed"}}> Total: {fmtCLP(cr.adminReceives)} · Ganancia neta: <span style={{color:"#059669"}}>{fmtCLP(cr.adminReceives-(cr.costPrice||0))}</span> </div> </div> <div style={{display:"flex",gap:6,flexShrink:0}}> <button style={{...S.btnPrimary,background:"#059669",fontSize:12,padding:"6px 14px"}} onClick={()=>approveCredit(cr)}>✓ Aprobar</button> <button style={{...S.btnGhost,color:"#dc2626",fontSize:12,padding:"6px 14px"}} onClick={()=>denyCredit(cr)}>✕ Denegar</button> </div> </div> ))} </div> </div> )} {/* ── Admin: panel de ventas con tarjeta pendientes ── */} {admin&&pendingCardSales.length>0&&( <div style={{background:"#eff6ff",border:"2px solid #93c5fd",borderRadius:14,padding:"14px 18px",marginBottom:20}}> <div style={{fontWeight:700,fontSize:13,color:"#1e40af",marginBottom:10}}> Solicitudes de venta con tarjeta ({pendingCardSales.length}) </div> <div style={{display:"flex",flexDirection:"column",gap:8}}> {pendingCardSales.map(cd=>( <div key={cd.id} style={{background:"#fff",border:"1px solid #bfdbfe",borderRadius:10,padding:"12px 14px",display:"flex",gap:12,alignItems:"center",flexWrap:"wrap"}}> <div style={{flex:1,minWidth:200}}> <div style={{fontWeight:700,fontSize:13,color:"#111827"}}>{cd.productName}</div> <div style={{fontSize:11,color:"#9ca3af"}}>{cd.productDesc}</div> <div style={{fontSize:11,color:"#6b7280",marginTop:4}}> {new Date(cd.createdAt).toLocaleString("es-CL",{day:"2-digit",month:"2-digit",year:"numeric",hour:"2-digit",minute:"2-digit"})} {" · "}{cd.requestedByName} </div> <div style={{display:"flex",gap:12,marginTop:6,flexWrap:"wrap",fontSize:12}}> <span style={{color:"#6b7280"}}>Valor producto: <strong style={{color:"#059669"}}>{fmtCLP(cd.salePrice)}</strong></span> <span style={{color:"#6b7280"}}>Recargo {cd.pct}%: <strong style={{color:"#2563eb"}}>{fmtCLP(cd.finalPrice)}</strong></span> {cd.useCuotas&&<span style={{color:"#6b7280"}}>Diferido: <strong>{cd.meses} meses</strong>{cd.valorMes?<> · <strong style={{color:"#1d4ed8"}}>{fmtCLP(cd.valorMes)}/mes</strong></>:""}</span>} </div> <div style={{marginTop:6,fontWeight:800,fontSize:14,color:"#1d4ed8"}}>


=== PAGE 31 ===
Total: {fmtCLP(cd.salePrice)} · Ganancia: <span style={{color:"#059669"}}>{fmtCLP(cd.salePrice-(cd.costPrice||0))}</span> </div> </div> <div style={{display:"flex",gap:6,flexShrink:0}}> <button style={{...S.btnPrimary,background:"#059669",fontSize:12,padding:"6px 14px"}} onClick={()=>approveCard(cd)}>✓ Aprobar</button> <button style={{...S.btnGhost,color:"#dc2626",fontSize:12,padding:"6px 14px"}} onClick={()=>denyCard(cd)}>✕ Denegar</button> </div> </div> ))} </div> </div> )} <div style={S.twoCol}> {/* ── Panel izquierdo: selector de modo ── */} <Card title="Seleccionar"> <div style={{display:"flex",gap:3,marginBottom:14,background:"#f3f4f6",padding:4,borderRadius:10,flexWrap:"wrap"}}> <button onClick={()=>{setMode("venta");setSelected(null);}} style={{...S.tab,...(mode==="venta"?S.tabOn:{}),flex:1,fontSize:11}}> Venta</button> <button onClick={()=>{setMode("credito");setCredSel(null);setCf(EMPTY_CRED);}} style={{...S.tab,...(mode==="credito"?{...S.tabOn,background:"#7c3aed",color:"#fff"}:{}),flex:1,fontSize:11}}> Crédito</button> <button onClick={()=>{setMode("tarjeta");setCardSel(null);setCardF(EMPTY_CARD);}} style={{...S.tab,...(mode==="tarjeta"?{...S.tabOn,background:"#1d4ed8",color:"#fff"}:{}),flex:1,fontSize:11}}> Tarjeta</button> <button onClick={()=>{setMode("gasto");setSelected(null);}} style={{...S.tab,...(mode==="gasto"?{...S.tabOn,color:"#dc2626"}:{}),flex:1,fontSize:11}}> Gasto</button> </div> {/* VENTA normal */} {mode==="venta"&&<> <input style={S.input} placeholder="Buscar por nombre o código…" value={search} onChange={e=>setSearch(e.target.value)}/> <div style={{marginTop:10,display:"flex",flexDirection:"column",gap:4,maxHeight:300,overflowY:"auto"}}> {available.map(p=>( <div key={p.id} onClick={()=>{setSelected(p);setSalePrice(p.salePrice);setImeiUsed("");}} style={{...S.row,cursor:"pointer",background:selected?.id===p.id?"#eff6ff":"#f9fafb",border:`1px solid ${selected?.id===p.id?"#93c5fd":"#e5e7eb"}`,borderRadius:8,transition:"all .15s",padding:"8px 10px"}}> <span style={{...S.mono,fontSize:11,color:"#9ca3af",width:72,flexShrink:0}}>{p.code}</span> <span style={{flex:1,fontSize:13,color:"#111827",fontWeight:500}}>{p.name}</span> <StockBadge n={p.stock} alert={p.lowStockAlert} sm/> </div> ))} {!available.length&&<p style={S.empty}>Sin productos disponibles</p>} </div> </>} {/* TARJETA */} {mode==="tarjeta"&&<> <div style={{fontSize:11,color:"#1d4ed8",background:"#eff6ff",border:"1px solid #bfdbfe",borderRadius:8,padding:"6px 10px",marginBottom:10}}> Selecciona cualquier producto para venta con tarjeta </div> <input style={S.input} placeholder="Buscar por nombre o código…" value={search} onChange={e=>setSearch(e.target.value)}/> <div style={{marginTop:8,display:"flex",flexDirection:"column",gap:4,maxHeight:240,overflowY:"auto"}}> {available.map(p=>(


=== PAGE 32 ===
<div key={p.id} onClick={()=>{setCardSel(p);setCardF(f=>({...f,salePrice:p.salePrice,finalPrice:calcCardFinal(p.salePrice,f.pct),imei:""}));}} style={{...S.row,cursor:"pointer",background:cardSel?.id===p.id?"#eff6ff":"#f9fafb",border:`1px solid ${cardSel?.id===p.id?"#93c5fd":"#e5e7eb"}`,borderRadius:8,padding:"8px 10px"}}> <span style={{...S.mono,fontSize:11,color:"#9ca3af",width:72,flexShrink:0}}>{p.code}</span> <span style={{flex:1,fontSize:13,color:"#111827",fontWeight:500}}>{p.name}</span> <StockBadge n={p.stock} alert={p.lowStockAlert} sm/> </div> ))} {!available.length&&<p style={S.empty}>Sin productos disponibles</p>} </div> {cardSel?.isPhone&&<> <label style={{...S.label,marginTop:8}}>IMEI a vender</label> <select style={S.select} value={cardF.imei||""} onChange={e=>setCardF(f=>({...f,imei:e.target.value}))}> <option value="">Seleccionar IMEI…</option> {cardSel.imeis.map(i=><option key={i} value={i}>{i}</option>)} </select> </>} </>} {/* CRÉDITO */} {mode==="credito"&&<> <div style={{fontSize:11,color:"#7c3aed",background:"#f5f3ff",border:"1px solid #ddd6fe",borderRadius:8,padding:"6px 10px",marginBottom:10}}> Solo teléfonos disponibles para venta a crédito </div> <div style={{display:"flex",flexDirection:"column",gap:4,maxHeight:220,overflowY:"auto",marginBottom:10}}> {phoneProducts.length===0&&<p style={S.empty}>Sin teléfonos en stock</p>} {phoneProducts.map(p=>( <div key={p.id} onClick={()=>{setCredSel(p);setCf(f=>({...f,salePrice:p.salePrice,imei:""}));}} style={{...S.row,cursor:"pointer",background:credSel?.id===p.id?"#f5f3ff":"#f9fafb",border:`1px solid ${credSel?.id===p.id?"#a78bfa":"#e5e7eb"}`,borderRadius:8,padding:"8px 10px"}}> <span style={{flex:1,fontSize:13,color:"#111827",fontWeight:500}}>{p.name}</span> <StockBadge n={p.stock} alert={p.lowStockAlert} sm/> </div> ))} </div> {credSel&&<> <label style={S.label}>IMEI a vender *</label> <select style={S.select} value={cf.imei} onChange={e=>setCf(f=>({...f,imei:e.target.value}))}> <option value="">Seleccionar IMEI…</option> {credSel.imeis.map(i=><option key={i} value={i}>{i}</option>)} </select> </>} </>} {/* GASTO */} {mode==="gasto"&&( <div style={{display:"flex",flexDirection:"column",gap:12}}> <div style={{background:"#fef2f2",border:"1px solid #fecaca",borderRadius:10,padding:"10px 12px",fontSize:12,color:"#dc2626"}}> Los gastos se restan de los ingresos del día en el resumen.


=== PAGE 33 ===
</div> <div> <label style={S.label}>Descripción del gasto</label> <input style={S.input} placeholder="ej: Arriendo local, Insumos…" value={expDesc} onChange={e=>setExpDesc(e.target.value)}/> </div> <div> <label style={S.label}>Monto</label> <input style={S.input} type="number" placeholder="0" value={expAmount} onChange={e=>setExpAmount(e.target.value)}/> </div> <button style={{...S.btnPrimary,background:"#dc2626",width:"100%"}} onClick={confirmExpense}> Registrar Gasto </button> </div> )} </Card> {/* ── Panel derecho: detalle ── */} <Card title={mode==="credito"?"Detalle Venta a Crédito":mode==="tarjeta"?"Detalle Venta con Tarjeta":mode==="gasto"?"Gastos del Día":"Detalle de Venta"}> {/* Detalle TARJETA */} {mode==="tarjeta"&&( cardSel ? ( <div style={{display:"flex",flexDirection:"column",gap:10}}> <div style={{background:"#eff6ff",border:"1px solid #bfdbfe",borderRadius:10,padding:"12px 14px"}}> <div style={{fontWeight:700,fontSize:14,color:"#1e3a8a"}}>{cardSel.name}</div> <div style={{fontSize:12,color:"#9ca3af",marginTop:2}}>{cardSel.description}</div> </div> <div style={S.fgrid}> <Fld label="Valor del producto *"> <input style={S.input} type="number" value={cardF.salePrice} onChange={e=>{const sp=e.target.value;setCardF(f=>({...f,salePrice:sp,finalPrice:calcCardFinal(sp,f.pct)}));}}/> </Fld> <Fld label={`Recargo % (${cardF.pct}%)`}> <input style={S.input} type="number" value={cardF.pct} onChange={e=>{const p=e.target.value;setCardF(f=>({...f,pct:p,finalPrice:calcCardFinal(f.salePrice,p)}));}}/> </Fld> <Fld label="Valor final con recargo" full> <input style={{...S.input,fontWeight:700,color:"#2563eb"}} type="number" value={cardF.finalPrice} onChange={e=>{ const fp=e.target.value; const pct=+cardF.pct||0; const sp = pct>0 ? Math.round((+fp||0)/(1+pct/100)) : (+fp||0); setCardF(f=>({...f,finalPrice:fp,salePrice:sp})); }} placeholder={String(calcCardFinal(cardF.salePrice,cardF.pct))}/> <div style={{fontSize:11,color:"#6b7280",marginTop:4}}>


=== PAGE 34 ===
Al modificar este valor se recalcula el precio del producto </div> </Fld> <Fld label="" full> <label style={{display:"flex",alignItems:"center",gap:8,cursor:"pointer",fontSize:13,color:"#374151",background:"#eff6ff",padding:"8px 10px",borderRadius:8}}> <input type="checkbox" checked={cardF.useCuotas} onChange={e=>setCardF(f=>({...f,useCuotas:e.target.checked}))} style={{width:15,height:15,accentColor:"#2563eb"}}/> Activar cuotas diferidas </label> </Fld> {cardF.useCuotas&&( <Fld label="Número de meses" full> <input style={S.input} type="number" value={cardF.meses} onChange={e=>setCardF(f=>({...f,meses:e.target.value}))} placeholder="ej: 12"/> {cardF.meses&&+cardF.meses>0&&(+cardF.finalPrice||calcCardFinal(cardF.salePrice,cardF.pct))>0&&( <div style={{marginTop:8,padding:"8px 12px",background:"#eff6ff",border:"1px solid #bfdbfe",borderRadius:8,display:"flex",justifyContent:"space-between",alignItems:"center"}}> <span style={{fontSize:12,color:"#1d4ed8"}}>Valor por mes</span> <span style={{fontSize:16,fontWeight:800,color:"#1d4ed8"}}> {fmtCLP(Math.ceil((+cardF.finalPrice||calcCardFinal(cardF.salePrice,cardF.pct))/(+cardF.meses)))} </span> </div> )} </Fld> )} </div> {/* Resumen */} {cardF.salePrice&&( <div style={{background:"#f0fdf4",border:"1px solid #86efac",borderRadius:10,padding:"10px 14px",fontSize:12}}> <div style={{fontWeight:700,color:"#166534",marginBottom:6}}>Resumen</div> <div style={{display:"flex",flexDirection:"column",gap:3,color:"#374151"}}> <div style={{display:"flex",justifyContent:"space-between"}}><span>Valor producto</span><span style={{fontWeight:600}}>{fmtCLP(+cardF.salePrice||0)}</span></div> <div style={{display:"flex",justifyContent:"space-between",color:"#2563eb"}}><span>+ Recargo ({cardF.pct}%)</span><span>+{fmtCLP((+cardF.finalPrice||calcCardFinal(cardF.salePrice,cardF.pct))-(+cardF.salePrice||0))}</span></div> <div style={{display:"flex",justifyContent:"space-between",borderTop:"1px solid #e5e7eb",paddingTop:4,marginTop:2,fontWeight:800,color:"#1d4ed8",fontSize:13}}> <span>Cobra al cliente</span><span>{fmtCLP(+cardF.finalPrice||calcCardFinal(cardF.salePrice,cardF.pct))}</span> </div> <div style={{display:"flex",justifyContent:"space-between",color:"#059669",fontWeight:700}}> <span>Total (sin recargo)</span><span>{fmtCLP(+cardF.salePrice||0)}</span> </div> {cardF.useCuotas&&cardF.meses&&+cardF.meses>0&&( <div style={{display:"flex",justifyContent:"space-between",color:"#1d4ed8",fontWeight:700}}> <span>Cuota mensual ({cardF.meses} meses)</span> <span>{fmtCLP(Math.ceil((+cardF.finalPrice||calcCardFinal(cardF.salePrice,cardF.pct))/(+cardF.meses)))}/mes</span> </div> )} </div> </div> )} <button style={{...S.btnPrimary,background:"#1d4ed8",width:"100%"}} onClick={submitCard}> Enviar Solicitud de Venta con Tarjeta


=== PAGE 35 ===
</button> <button style={{...S.btnGhost,width:"100%"}} onClick={()=>{setCardSel(null);setCardF(EMPTY_CARD);}}>Cancelar</button> </div> ) : ( <div style={{textAlign:"center",padding:"40px 20px"}}> <div style={{fontSize:40}}> </div> <p style={{color:"#9ca3af",marginTop:10,fontSize:13}}>Selecciona un producto para venta con tarjeta</p> </div> ) )} {/* Detalle venta normal */} {mode==="venta"&&selected&&( <> <div style={{background:"#f9fafb",border:"1px solid #e5e7eb",borderRadius:10,padding:14,marginBottom:14}}> <div style={{fontWeight:700,fontSize:14,color:"#111827"}}>{selected.name}</div> <div style={{fontSize:12,color:"#9ca3af",marginTop:2}}>{selected.description}</div> <div style={{display:"flex",gap:14,marginTop:8,flexWrap:"wrap"}}> {admin&&<span style={{fontSize:12,color:"#6b7280"}}>Costo: <strong>{fmtCLP(selected.costPrice)}</strong></span>} <span style={{fontSize:12,color:"#059669"}}>Precio venta: <strong>{fmtCLP(selected.salePrice)}</strong></span> </div> {selected.minPrice>0&&( <div style={{marginTop:6,fontSize:11,background:"#fef3c7",color:"#92400e",borderRadius:6,padding:"4px 8px",display:"inline-block"}}> Precio mínimo sugerido: {fmtCLP(selected.minPrice)} </div> )} </div> <label style={S.label}>Precio de Venta</label> <input style={S.input} type="number" value={salePrice} onChange={e=>setSalePrice(e.target.value)}/> {selected.isPhone&&<> <label style={{...S.label,marginTop:12}}>IMEI a vender</label> <select style={S.select} value={imeiUsed} onChange={e=>setImeiUsed(e.target.value)}> <option value="">Seleccionar IMEI…</option> {selected.imeis.map(i=><option key={i} value={i}>{i}</option>)} </select> </>} <label style={{...S.label,marginTop:12}}>Método de pago</label> <div style={{display:"flex",gap:6,marginBottom:2}}> <button onClick={()=>setPayMethod("efectivo")} style={{flex:1,padding:"8px 0",borderRadius:8,border:`2px solid ${payMethod==="efectivo"?"#2563eb":"#e5e7eb"}`,background:payMethod==="efectivo"?"#eff6ff":"#f9fafb",color:payMethod==="efectivo"?"#1d4ed8":"#6b7280",fontWeight:600,fontSize:13,cursor:"pointer",transition:"all .15s"}}> Efectivo </button> <button onClick={()=>setPayMethod("transferencia")} style={{flex:1,padding:"8px 0",borderRadius:8,border:`2px solid ${payMethod==="transferencia"?"#7c3aed":"#e5e7eb"}`,background:payMethod==="transferencia"?"#f5f3ff":"#f9fafb",color:payMethod==="transferencia"?"#7c3aed":"#6b7280",fontWeight:600,fontSize:13,cursor:"pointer",transition:"all .15s"}}> Transferencia </button> </div>


=== PAGE 36 ===
<button style={{...S.btnPrimary,width:"100%",marginTop:14,fontSize:14}} onClick={confirmSale}>✓ Confirmar Venta</button> <button style={{...S.btnGhost,width:"100%",marginTop:6}} onClick={()=>{setSelected(null);setSearch("");}}>Cancelar</button> </> )} {mode==="venta"&&!selected&&( <div style={{textAlign:"center",padding:"40px 20px"}}> <div style={{fontSize:40}}> </div> <p style={{color:"#9ca3af",marginTop:10,fontSize:13}}>Selecciona un producto para registrar la venta</p> </div> )} {/* Detalle crédito */} {mode==="credito"&&( credSel ? ( <div style={{display:"flex",flexDirection:"column",gap:10}}> <div style={{background:"#f5f3ff",border:"1px solid #ddd6fe",borderRadius:10,padding:"12px 14px"}}> <div style={{fontWeight:700,fontSize:14,color:"#4c1d95"}}>{credSel.name}</div> <div style={{fontSize:12,color:"#9ca3af",marginTop:2}}>{credSel.description}</div> </div> <div style={S.fgrid}> <Fld label="Precio de Venta *"> <input style={S.input} type="number" value={cf.salePrice} onChange={e=>setCf(f=>({...f,salePrice:e.target.value}))}/> </Fld> <Fld label="Entrada Pedida *"> <input style={S.input} type="number" value={cf.entradaPedida} onChange={e=>setCf(f=>({...f,entradaPedida:e.target.value}))}/> </Fld> <Fld label="Entrada que Dio *"> <input style={S.input} type="number" value={cf.entradaDada} onChange={e=>setCf(f=>({...f,entradaDada:e.target.value}))}/> </Fld> <Fld label={`Retención % (${cf.retencion}%)`}> <input style={S.input} type="number" value={cf.retencion} onChange={e=>setCf(f=>({...f,retencion:e.target.value}))}/> </Fld> <Fld label="" full> <label style={{display:"flex",alignItems:"center",gap:8,cursor:"pointer",fontSize:13,color:"#374151",background:"#f5f3ff",padding:"8px 10px",borderRadius:8}}> <input type="checkbox" checked={cf.useWeeks} onChange={e=>setCf(f=>({...f,useWeeks:e.target.checked}))} style={{width:15,height:15,accentColor:"#7c3aed"}}/> Activar semanas perdonadas </label> </Fld> {cf.useWeeks&&<> <Fld label="Semanas Perdonadas"> <input style={S.input} type="number" value={cf.semanasPerdonadas} onChange={e=>setCf(f=>({...f,semanasPerdonadas:e.target.value}))}/> </Fld> <Fld label="Valor a Pagar por Semana"> <input style={S.input} type="number" value={cf.valorSemana} onChange={e=>setCf(f=>({...f,valorSemana:e.target.value}))}/> </Fld> </>} </div>


=== PAGE 37 ===
{/* Resumen cálculo */} {cf.salePrice&&( <div style={{background:"#f0fdf4",border:"1px solid #86efac",borderRadius:10,padding:"10px 14px",fontSize:12}}> <div style={{fontWeight:700,color:"#166534",marginBottom:6}}>Resumen de la operación</div> <div style={{display:"flex",flexDirection:"column",gap:3,color:"#374151"}}> <div style={{display:"flex",justifyContent:"space-between"}}><span>Precio venta</span><span style={{fontWeight:600}}>{fmtCLP(+cf.salePrice||0)}</span></div> <div style={{display:"flex",justifyContent:"space-between",color:"#dc2626"}}><span>— Entrada pedida</span><span>-{fmtCLP(+cf.entradaPedida||0)}</span></div> <div style={{display:"flex",justifyContent:"space-between",color:"#2563eb"}}><span>+ Entrada dada</span><span>+{fmtCLP(+cf.entradaDada||0)}</span></div> {cf.useWeeks&&<div style={{display:"flex",justifyContent:"space-between",color:"#d97706"}}><span>— Semanas perdonadas ({cf.semanasPerdonadas||0} × {fmtCLP(+cf.valorSemana||0)})</span><span>-{fmtCLP((+cf.semanasPerdonadas||0)*(+cf.valorSemana||0))}</span></div>} <div style={{display:"flex",justifyContent:"space-between",color:"#dc2626"}}><span>— Retención ({cf.retencion}%)</span><span>-{fmtCLP((+cf.salePrice||0)*((+cf.retencion||0)/100))}</span></div> <div style={{display:"flex",justifyContent:"space-between",borderTop:"1px solid #e5e7eb",paddingTop:4,marginTop:2,fontWeight:800,color:"#7c3aed",fontSize:13}}> <span>Total</span><span>{fmtCLP(calcAdminGets(cf,credSel))}</span> </div> {admin&&( <div style={{display:"flex",justifyContent:"space-between",fontWeight:700,color:"#059669",fontSize:12}}> <span>Ganancia neta (−costo producto)</span><span>{fmtCLP(calcAdminGets(cf,credSel)-(credSel.costPrice||0))}</span> </div> )} {!admin&&(()=>{ const netProfit=calcAdminGets(cf,credSel)-(credSel.costPrice||0); const approved=netProfit>10; return ( <div style={{marginTop:8,padding:"8px 12px",borderRadius:8,background:approved?"#dcfce7":"#fee2e2",border:`1px solid ${approved?"#86efac":"#fca5a5"}`,display:"flex",alignItems:"center",gap:8}}> <span style={{fontSize:16}}>{approved?" ":" "}</span> <span style={{fontWeight:700,fontSize:13,color:approved?"#166534":"#991b1b"}}> {approved?"Venta pre aprobada":"Venta pre denegada"} </span> </div> ); })()} </div> </div> )} <button style={{...S.btnPrimary,background:"#7c3aed",width:"100%"}} onClick={submitCredit}> Enviar Solicitud de Crédito </button> <button style={{...S.btnGhost,width:"100%"}} onClick={()=>{setCredSel(null);setCf(EMPTY_CRED);}}>Cancelar</button> </div> ) : ( <div style={{textAlign:"center",padding:"40px 20px"}}> <div style={{fontSize:40}}> </div> <p style={{color:"#9ca3af",marginTop:10,fontSize:13}}>Selecciona un teléfono para iniciar una venta a crédito</p> </div> ) )} {/* Gastos del día */}


=== PAGE 38 ===
{mode==="gasto"&&( todayExpenses.length>0 ? ( <div> <div style={{fontSize:11,fontWeight:700,color:"#9ca3af",textTransform:"uppercase",letterSpacing:".06em",marginBottom:10}}> {admin?"Gastos registrados hoy":"Mis gastos de hoy"} </div> <div style={{display:"flex",flexDirection:"column",gap:6,maxHeight:300,overflowY:"auto"}}> {todayExpenses.map(e=>( <div key={e.id} style={{display:"flex",alignItems:"center",gap:8,background:"#fef2f2",border:"1px solid #fecaca",borderRadius:8,padding:"8px 12px"}}> <div style={{flex:1}}> <div style={{fontSize:13,fontWeight:600,color:"#111827"}}>{e.description}</div> <div style={{fontSize:11,color:"#9ca3af"}}>{e.userName}</div> </div> <span style={{fontWeight:700,color:"#dc2626",fontSize:13,flexShrink:0}}>{fmtCLP(e.amount)}</span> <button onClick={()=>deleteExpense(e.id)} style={{background:"transparent",border:"none",cursor:"pointer",color:"#9ca3af",fontSize:14,padding:"2px 4px"}}>✕</button> </div> ))} </div> <div style={{marginTop:10,padding:"8px 12px",background:"#f9fafb",borderRadius:8,display:"flex",justifyContent:"space-between"}}> <span style={{fontSize:12,color:"#6b7280"}}>{admin?"Total gastos":"Mis gastos"}</span> <span style={{fontWeight:800,color:"#dc2626"}}>{fmtCLP(totalExpenses)}</span> </div> </div> ):( <div style={{textAlign:"center",padding:"40px 20px"}}> <div style={{fontSize:40}}> </div> <p style={{color:"#9ca3af",marginTop:10,fontSize:13}}>Sin gastos registrados para esta fecha</p> </div> ) )} </Card> </div> {/* ── Resumen del día ── */} <div style={{display:"grid",gridTemplateColumns:"repeat(auto-fill,minmax(160px,1fr))",gap:10,margin:"16px 0"}}> <div style={{background:"#fff",border:"1px solid #e5e7eb",borderRadius:12,padding:"14px 16px",borderLeft:"3px solid #2563eb"}}> <div style={{fontSize:11,fontWeight:700,color:"#9ca3af",textTransform:"uppercase",marginBottom:4}}>{admin?"Ingresos totales":"Mis ingresos"}</div> <div style={{fontSize:20,fontWeight:800,color:"#2563eb"}}>{fmtCLP(totalRevenue)}</div> </div> {admin&&<> <div style={{background:"#fff",border:"1px solid #e5e7eb",borderRadius:12,padding:"14px 16px",borderLeft:"3px solid #dc2626"}}> <div style={{fontSize:11,fontWeight:700,color:"#9ca3af",textTransform:"uppercase",marginBottom:4}}>Gastos</div> <div style={{fontSize:20,fontWeight:800,color:"#dc2626"}}>-{fmtCLP(totalExpenses)}</div> </div> <div style={{background:"#fff",border:"1px solid #e5e7eb",borderRadius:12,padding:"14px 16px",borderLeft:`3px solid ${netDay>=0?"#059669":"#dc2626"}`}}> <div style={{fontSize:11,fontWeight:700,color:"#9ca3af",textTransform:"uppercase",marginBottom:4}}>Ganancias del día</div> <div style={{fontSize:20,fontWeight:800,color:netDay>=0?"#059669":"#dc2626"}}>{fmtCLP(netDay)}</div>


=== PAGE 39 ===
</div> </>} </div> <Card title={`Movimientos del día — ${saleDate}`}> <div style={S.tableWrap}> <table style={S.table}> <thead><tr style={S.thead}> {["Hora","Descripción","Tipo","Pago","Vendedor",...(admin?["Costo"]:[]),"Ingreso","Gasto",...(admin?["Ganancia",""]:[])].map(h=><th key={h} style={S.th}>{h}</th>)} </tr></thead> <tbody> {todaySales.map(s=>( <tr key={s.id} style={S.tr}> <td style={S.td}>{new Date(+s.id.slice(1)).toLocaleTimeString("es-CL",{hour:"2-digit",minute:"2-digit"})}</td> <td style={S.td}><span style={{fontWeight:500,color:"#111827"}}>{s.productName}</span>{s.imei&&<div style={{fontSize:10,color:"#9ca3af"}}>{s.imei}</div>}</td> <td style={S.td}> <span style={{background:s.isCredit?"#f5f3ff":"#dbeafe",color:s.isCredit?"#6d28d9":"#1d4ed8",padding:"2px 8px",borderRadius:20,fontSize:11,fontWeight:600}}> {s.isCredit?" Crédito":"Venta"} </span> </td> <td style={S.td}> <span style={{fontSize:12,fontWeight:600,color:s.payMethod==="transferencia"?"#7c3aed":"#059669"}}> {s.payMethod==="transferencia"?" Transf.":" Efectivo"} </span> </td> <td style={S.td}>{s.userName}</td> {admin&&<td style={S.td}>{fmtCLP(s.costPrice)}</td>} <td style={S.td}><span style={{fontWeight:700,color:"#059669"}}>{fmtCLP(s.salePrice)}</span></td> <td style={S.td}>—</td> {admin&&<td style={S.td}><span style={{color:"#d97706",fontWeight:600}}>{fmtCLP(s.salePrice-s.costPrice)}</span></td>} {admin&&<td style={S.td}><button onClick={()=>deleteSale(s.id)} style={{background:"#fee2e2",border:"1px solid #fca5a5",color:"#dc2626",borderRadius:6,padding:"2px 8px",cursor:"pointer",fontSize:11,fontWeight:600}}>✕</button></td>} </tr> ))} {todayExpenses.map(e=>( <tr key={e.id} style={{...S.tr,background:"#fff5f5"}}> <td style={S.td}>{new Date(+e.id.slice(1)).toLocaleTimeString("es-CL",{hour:"2-digit",minute:"2-digit"})}</td> <td style={S.td}><span style={{fontWeight:500,color:"#dc2626"}}>{e.description}</span></td> <td style={S.td}><span style={{background:"#fee2e2",color:"#dc2626",padding:"2px 8px",borderRadius:20,fontSize:11,fontWeight:600}}>Gasto</span></td> <td style={S.td}>—</td> <td style={S.td}>{e.userName}</td> {admin&&<td style={S.td}>—</td>} <td style={S.td}>—</td> <td style={S.td}><span style={{fontWeight:700,color:"#dc2626"}}>{fmtCLP(e.amount)}</span></td> {admin&&<td style={S.td}>—</td>} {admin&&<td style={S.td}></td>} </tr> ))}


=== PAGE 40 ===
</tbody> </table> {!todaySales.length&&!todayExpenses.length&&<p style={{...S.empty,textAlign:"center",padding:24}}>Sin movimientos para esta fecha</p>} </div> </Card> </Page> ); } /* ─── HISTORIAL ──────────────────────────────────────────────────────────── */ function Historial({ sales, setSales, products, setProducts, session, admin }) { const [search, setSearch] = useState(""); const [dateFrom, setDateFrom] = useState(() => { const d = new Date(); d.setDate(d.getDate()-30); return d.toISOString().split("T")[0]; }); const [dateTo, setDateTo] = useState(today()); // Todos los usuarios ven todas las ventas en el historial const base = sales; // Excluir el día de hoy (para ver solo días anteriores en vendedor; admin puede ver todo) const filtered = base.filter(s => { const inRange = s.date >= dateFrom && s.date <= dateTo; const matchSearch = !search || s.productName?.toLowerCase().includes(search.toLowerCase()) || (s.imei||"").includes(search) || s.userName?.toLowerCase().includes(search.toLowerCase()); return inRange && matchSearch; }).slice().sort((a,b) => b.id.localeCompare(a.id)); // más reciente primero const getProduct = id => products.find(p => p.id === id); // Agrupar por fecha para mostrar separadores const byDate = filtered.reduce((acc, s) => { if (!acc[s.date]) acc[s.date] = []; acc[s.date].push(s); return acc; }, {}); return ( <Page title="Historial de Ventas" sub={`${filtered.length} ventas en el período seleccionado`} > {/* Filtros */}


=== PAGE 41 ===
<div style={{display:"flex",gap:8,marginBottom:16,flexWrap:"wrap",alignItems:"flex-end"}}> <div style={{flex:1,minWidth:200}}> <label style={S.label}>Buscar</label> <input style={S.input} placeholder="Producto, IMEI, vendedor…" value={search} onChange={e=>setSearch(e.target.value)} /> </div> <div> <label style={S.label}>Desde</label> <input type="date" style={{...S.input,width:"auto"}} value={dateFrom} onChange={e=>setDateFrom(e.target.value)} /> </div> <div> <label style={S.label}>Hasta</label> <input type="date" style={{...S.input,width:"auto"}} value={dateTo} onChange={e=>setDateTo(e.target.value)} /> </div> </div> {/* Sin resultados */} {Object.keys(byDate).length === 0 && ( <div style={{textAlign:"center",padding:"48px 20px",background:"#fff",borderRadius:14,border:"1px solid #e5e7eb"}}> <div style={{fontSize:40}}> </div> <p style={{color:"#9ca3af",marginTop:10,fontSize:13}}>No hay ventas en el período seleccionado</p> </div> )} {/* Agrupado por fecha */} {Object.entries(byDate).map(([date, daySales]) => ( <div key={date} style={{marginBottom:20}}> {/* Encabezado de fecha */} <div style={{display:"flex",alignItems:"center",gap:10,marginBottom:10}}> <div style={{fontSize:12,fontWeight:700,color:"#6b7280",background:"#f3f4f6",padding:"4px 12px",borderRadius:20,whiteSpace:"nowrap"}}> {new Date(date+"T12:00:00").toLocaleDateString("es-CL",{weekday:"long",day:"numeric",month:"long",year:"numeric"})} </div> <div style={{flex:1,height:1,background:"#e5e7eb"}} /> <div style={{fontSize:11,color:"#9ca3af",whiteSpace:"nowrap"}}>{daySales.length} venta{daySales.length!==1?"s":""}</div> </div> {/* Tarjetas de ventas del día */} <div style={{display:"flex",flexDirection:"column",gap:8}}> {daySales.map(s => { const prod = getProduct(s.productId); const hora = new Date(+s.id.slice(1)).toLocaleTimeString("es-CL",{hour:"2-digit",minute:"2-digit"}); return ( <div key={s.id} style={{background:"#fff",border:"1px solid #e5e7eb",borderRadius:12,padding:"14px 16px",display:"flex",gap:14,alignItems:"flex-start",flexWrap:"wrap"}}> {/* Hora */} <div style={{flexShrink:0,textAlign:"center",background:"#f0f9ff",borderRadius:8,padding:"6px 10px",minWidth:52}}> <div style={{fontSize:13,fontWeight:700,color:"#1d4ed8"}}>{hora}</div> <div style={{fontSize:9,color:"#93c5fd",textTransform:"uppercase",letterSpacing:".04em",marginTop:1}}>hora</div> </div>


=== PAGE 42 ===
{/* Info producto */} <div style={{flex:1,minWidth:160}}> <div style={{fontWeight:700,fontSize:14,color:"#111827"}}>{s.productName}</div> {prod && ( <div style={{fontSize:12,color:"#9ca3af",marginTop:2}}>{prod.description}</div> )} {s.imei && ( <div style={{marginTop:5}}> <code style={{...S.mono,fontSize:11,color:"#1d4ed8"}}>IMEI: {s.imei}</code> </div> )} {/* Método de pago */} <div style={{marginTop:6}}> <span style={{fontSize:11,fontWeight:600,padding:"2px 8px",borderRadius:12,background:s.payMethod==="transferencia"?"#f5f3ff":"#f0fdf4",color:s.payMethod==="transferencia"?"#7c3aed":"#059669"}}> {s.payMethod==="transferencia"?" Transferencia":" Efectivo"} </span> </div> </div> {/* Vendedor */} <div style={{flexShrink:0,textAlign:"right"}}> <div style={{fontSize:11,color:"#9ca3af",marginBottom:2}}>Vendedor</div> <div style={{fontSize:13,fontWeight:600,color:"#374151"}}>{s.userName}</div> </div> {/* Precio */} <div style={{flexShrink:0,textAlign:"right"}}> <div style={{fontSize:11,color:"#9ca3af",marginBottom:2}}>Precio de venta</div> <div style={{fontSize:16,fontWeight:800,color:"#059669"}}>{fmtCLP(s.salePrice)}</div> </div> {/* Eliminar — solo admin */} {admin&&( <button onClick={()=>{ if(s.imei){ setProducts(products.map(p=>{ if(p.id!==s.productId)return p; return {...p,stock:p.stock+1,imeis:[...p.imeis,s.imei]}; })); } else { setProducts(products.map(p=>p.id===s.productId?{...p,stock:p.stock+1}:p)); } setSales(sales.filter(x=>x.id!==s.id)); }} style={{flexShrink:0,background:"#fee2e2",border:"1px solid #fca5a5",color:"#dc2626",borderRadius:8,padding:"4px 10px",cursor:"pointer",fontSize:12,fontWeight:600,alignSelf:"flex-start"}}> ✕


=== PAGE 43 ===
</button> )} </div> ); })} </div> </div> ))} </Page> ); } /* ─── STOCK BAJO ─────────────────────────────────────────────────────────── */ function LowStock({products,cats,brands}) { const out=products.filter(p=>p.stock===0); const low=products.filter(p=>p.stock>0&&p.stock<=p.lowStockAlert); const catName=id=>cats.find(c=>c.id===id)?.name||""; const brandName=id=>brands.find(b=>b.id===id)?.name||""; const Row=({p})=>( <div style={{display:"flex",alignItems:"center",gap:12,padding:"10px 0",borderBottom:"1px solid #f3f4f6"}}> <code style={{...S.mono,fontSize:11,color:"#9ca3af",width:72,flexShrink:0}}>{p.code}</code> <div style={{flex:1}}> <div style={{fontWeight:600,color:"#111827",fontSize:13}}>{p.name}</div> <div style={{fontSize:11,color:"#9ca3af"}}>{catName(p.catId)} · {brandName(p.brandId)}</div> </div> <StockBadge n={p.stock} alert={p.lowStockAlert}/> </div> ); return ( <Page title="Alertas de Stock" sub="Productos con existencias reducidas o agotadas"> <div style={{display:"flex",flexDirection:"column",gap:16}}> <div style={{background:"#fff",border:"1px solid #e5e7eb",borderLeft:"4px solid #dc2626",borderRadius:14,padding:"18px 20px"}}> <div style={{fontWeight:700,fontSize:14,color:"#dc2626",marginBottom:14}}> Sin existencias ({out.length})</div> {out.length?out.map(p=><Row key={p.id} p={p}/>):<p style={S.empty}>¡Todos los productos tienen stock! ✓</p>} </div> <div style={{background:"#fff",border:"1px solid #e5e7eb",borderLeft:"4px solid #d97706",borderRadius:14,padding:"18px 20px"}}> <div style={{fontWeight:700,fontSize:14,color:"#d97706",marginBottom:14}}> Stock reducido ({low.length})</div> {low.length?low.map(p=><Row key={p.id} p={p}/>):<p style={S.empty}>Todos superan el umbral mínimo ✓</p>} </div> </div> </Page> ); } /* ─── REPORTES ───────────────────────────────────────────────────────────── */ /* Mini bar chart — pure CSS, no lib needed */


=== PAGE 44 ===
function BarChart({data, color, labelFn, valFn, fmtVal}) { const max = Math.max(...data.map(valFn), 1); return ( <div style={{display:"flex",alignItems:"flex-end",gap:4,height:160,padding:"0 4px",overflowX:"auto"}}> {data.map((d,i)=>{ const h = Math.round((valFn(d)/max)*140); return ( <div key={i} style={{display:"flex",flexDirection:"column",alignItems:"center",gap:4,minWidth:36,flex:1}}> <div style={{fontSize:10,color:color,fontWeight:700,whiteSpace:"nowrap"}}>{h>0?fmtVal(valFn(d)):""}</div> <div style={{width:"100%",background:color,borderRadius:"4px 4px 0 0",height:h||2,transition:"height .4s",opacity:.85}}/> <div style={{fontSize:9,color:"#9ca3af",textAlign:"center",whiteSpace:"nowrap",overflow:"hidden",textOverflow:"ellipsis",maxWidth:50}}>{labelFn(d)}</div> </div> ); })} {data.length===0&&<div style={{color:"#9ca3af",fontSize:13,margin:"auto"}}>Sin datos para el período</div>} </div> ); } function ChartView({title, color, icon, onBack, children, granularity, setGranularity, dateFilter, setDateFilter}) { return ( <div> <div style={{display:"flex",alignItems:"center",gap:10,marginBottom:16,flexWrap:"wrap"}}> <button onClick={onBack} style={{background:"#f3f4f6",border:"1px solid #e5e7eb",borderRadius:8,padding:"6px 12px",cursor:"pointer",fontSize:13,fontWeight:600,color:"#374151",display:"flex",alignItems:"center",gap:6}}> ← Volver </button> <span style={{fontSize:18,fontWeight:800,color,display:"flex",alignItems:"center",gap:6}}>{icon} {title}</span> </div> {/* Granularity selector */} <div style={{display:"flex",gap:6,marginBottom:14,flexWrap:"wrap",alignItems:"center"}}> <div style={{display:"flex",gap:4,background:"#f3f4f6",padding:3,borderRadius:8}}> {["hora","día","mes","año"].map(g=>( <button key={g} onClick={()=>setGranularity(g)} style={{padding:"5px 12px",borderRadius:6,border:"none",cursor:"pointer",fontSize:12,fontWeight:600, background:granularity===g?color:"transparent",color:granularity===g?"#fff":"#6b7280",transition:"all .15s"}}> {g.charAt(0).toUpperCase()+g.slice(1)} </button> ))} </div> {/* Date range filter */} {granularity==="hora"&&( <div style={{display:"flex",gap:6,alignItems:"center"}}> <span style={{fontSize:12,color:"#6b7280"}}>Fecha:</span> <input type="date" style={{fontSize:12,padding:"5px 8px",border:"1px solid #e5e7eb",borderRadius:6}} value={dateFilter.day||today()} onChange={e=>setDateFilter({...dateFilter,day:e.target.value})}/> </div> )} {granularity==="día"&&(


=== PAGE 45 ===
<div style={{display:"flex",gap:6,alignItems:"center",flexWrap:"wrap"}}> <span style={{fontSize:12,color:"#6b7280"}}>De:</span> <input type="date" style={{fontSize:12,padding:"5px 8px",border:"1px solid #e5e7eb",borderRadius:6}} value={dateFilter.from||today()} onChange={e=>setDateFilter({...dateFilter,from:e.target.value})}/> <span style={{fontSize:12,color:"#6b7280"}}>a:</span> <input type="date" style={{fontSize:12,padding:"5px 8px",border:"1px solid #e5e7eb",borderRadius:6}} value={dateFilter.to||today()} onChange={e=>setDateFilter({...dateFilter,to:e.target.value})}/> </div> )} {granularity==="mes"&&( <div style={{display:"flex",gap:6,alignItems:"center"}}> <span style={{fontSize:12,color:"#6b7280"}}>Año:</span> <input type="number" style={{fontSize:12,padding:"5px 8px",border:"1px solid #e5e7eb",borderRadius:6,width:80}} value={dateFilter.year||new Date().getFullYear()} onChange={e=>setDateFilter({...dateFilter,year:+e.target.value})}/> </div> )} {granularity==="año"&&( <div style={{display:"flex",gap:6,alignItems:"center"}}> <span style={{fontSize:12,color:"#6b7280"}}>Desde año:</span> <input type="number" style={{fontSize:12,padding:"5px 8px",border:"1px solid #e5e7eb",borderRadius:6,width:80}} value={dateFilter.yearFrom||new Date().getFullYear()-4} onChange={e=>setDateFilter({...dateFilter,yearFrom:+e.target.value})}/> <span style={{fontSize:12,color:"#6b7280"}}>a:</span> <input type="number" style={{fontSize:12,padding:"5px 8px",border:"1px solid #e5e7eb",borderRadius:6,width:80}} value={dateFilter.yearTo||new Date().getFullYear()} onChange={e=>setDateFilter({...dateFilter,yearTo:+e.target.value})}/> </div> )} </div> <div style={{background:"#fff",border:"1px solid #e5e7eb",borderRadius:14,padding:"20px 16px"}}> {children} </div> </div> ); } function Reports({sales,products,users,cats,expenses,creditSales,cardSales}) { const [from,setFrom]=useState(today()); const [to,setTo]=useState(today()); const [activeChart,setActiveChart]=useState(null); // which stat card was clicked const [granularity,setGranularity]=useState("día"); const [dateFilter,setDateFilter]=useState({day:today(),from:today(),to:today(),year:new Date().getFullYear(),yearFrom:new Date().getFullYear()-4,yearTo:new Date().getFullYear()}); const f=sales.filter(s=>s.date>=from&&s.date<=to); const fExp=expenses.filter(e=>e.date>=from&&e.date<=to); const rev=f.reduce((a,s)=>a+s.salePrice,0), cost=f.reduce((a,s)=>a+s.costPrice,0); const totalExp=fExp.reduce((a,e)=>a+e.amount,0); const profit=rev-cost-totalExp; const bySeller=users.map(u=>({...u,count:f.filter(s=>s.userId===u.id).length,rev:f.filter(s=>s.userId===u.id).reduce((a,s)=>a+s.salePrice,0)})).filter(u=>u.count>0).sort((a,b)=>b.rev-a.rev); const byCat=cats.map(c=>({...c,count:f.filter(s=>s.catName===c.name).length,rev:f.filter(s=>s.catName===c.name).reduce((a,s)=>a+s.salePrice,0)})).filter(c=>c.count>0); const approvedCredits=(creditSales||[]).filter(c=>c.status==="approved"&&c.approvedAt>=from&&c.approvedAt<=to); const totalCreditAdmin=approvedCredits.reduce((a,c)=>a+(c.adminReceives-(c.costPrice||0)),0); const totalRetenciones=(creditSales||[]).filter(c=>c.status==="approved").reduce((a,c)=>a+c.retentionAmount,0); const pendingCredits=(creditSales||[]).filter(c=>c.status==="pending"); const approvedCards=(cardSales||[]).filter(c=>c.status==="approved"&&c.approvedAt>=from&&c.approvedAt<=to);


=== PAGE 46 ===
const totalCardAdmin=approvedCards.reduce((a,c)=>a+(c.salePrice-(c.costPrice||0)),0); // ── Helper: agrupar datos por granularidad ───────────────────────────── const groupData = (items, getDate, getValue) => { try { const groups = {}; const g = granularity; const df = dateFilter; items.forEach(item => { try { const d = getDate(item); if (!d || typeof d !== "string") return; let key = ""; if (g==="hora") { if (d.slice(0,10) !== (df.day||"")) return; const ts = item.id ? (+String(item.id).replace(/\D/g,"")||0) : (item.createdAt||0); if (!ts) return; key = new Date(ts).toLocaleTimeString("es-CL",{hour:"2-digit",minute:"2-digit"}).slice(0,2)+"h"; } else if (g==="día") { if (!df.from||!df.to||d<df.from||d>df.to) return; key = d; } else if (g==="mes") { const yr = d.slice(0,4); if (+yr!==+(df.year||0)) return; key = d.slice(0,7); } else if (g==="año") { const yr = +d.slice(0,4); if (yr<(+(df.yearFrom||0))||yr>(+(df.yearTo||9999))) return; key = String(yr); } if (!key) return; if (!groups[key]) groups[key]=0; groups[key]+=getValue(item)||0; } catch {} }); return Object.entries(groups).sort(([a],[b])=>a.localeCompare(b)).map(([label,value])=>({label,value})); } catch { return []; } }; const fmtLabel = label => { if (granularity==="día") return label.slice(5); // MM-DD if (granularity==="mes") return ["Ene","Feb","Mar","Abr","May","Jun","Jul","Ago","Sep","Oct","Nov","Dic"][+label.slice(5,7)-1]; return label; }; // ── Vista de gráfico detallado ───────────────────────────────────────── const statCards = [ {key:"ventas", color:"#2563eb", icon:" ", label:"Ventas", val:f.length, sub:"transacciones"},


=== PAGE 47 ===
{key:"ingresos", color:"#059669", icon:" ", label:"Ingresos", val:fmtCLP(rev), sub:"total vendido"}, {key:"costo", color:"#d97706", icon:" ", label:"Costo mercancía", val:fmtCLP(cost), sub:"costo de lo vendido"}, {key:"gastos", color:"#dc2626", icon:" ", label:"Gastos", val:fmtCLP(totalExp), sub:`${fExp.length} registros`}, {key:"ganancia", color:profit>=0?"#7c3aed":"#dc2626", icon:" ", label:"Ganancia neta", val:fmtCLP(profit), sub:"ingresos - costos - gastos"}, {key:"credito", color:"#0ea5e9", icon:" ", label:"Ganancias crédito", val:fmtCLP(totalCreditAdmin), sub:`${approvedCredits.length} créditos aprobados`}, {key:"retenciones", color:"#a16207", icon:" ", label:"Total retenciones", val:fmtCLP(totalRetenciones), sub:"acumulado histórico créditos"}, {key:"tarjeta", color:"#0369a1", icon:" ", label:"Ganancias tarjeta", val:fmtCLP(totalCardAdmin), sub:`${approvedCards.length} ventas con tarjeta`}, ]; if (activeChart) { const chartConfigs = { ventas: { title:"Ventas", color:"#2563eb", icon:" ", fmtVal:v=>`${v}`, desc:"Número de ventas realizadas", data: groupData(sales.filter(s=>s.date>=from&&s.date<=to), s=>s.date, ()=>1) }, ingresos: { title:"Ingresos", color:"#059669", icon:" ", fmtVal:v=>fmtCLP(v), desc:"Total de ingresos por ventas", data: groupData(sales.filter(s=>s.date>=from&&s.date<=to), s=>s.date, s=>s.salePrice) }, costo: { title:"Costo mercancía", color:"#d97706", icon:" ", fmtVal:v=>fmtCLP(v), desc:"Costo de los productos vendidos", data: groupData(sales.filter(s=>s.date>=from&&s.date<=to), s=>s.date, s=>s.costPrice||0) }, gastos: { title:"Gastos", color:"#dc2626", icon:" ", fmtVal:v=>fmtCLP(v), desc:"Gastos operativos registrados", data: groupData(expenses.filter(e=>e.date>=from&&e.date<=to), e=>e.date, e=>e.amount) }, ganancia: { title:"Ganancia neta", color:"#7c3aed", icon:" ", fmtVal:v=>fmtCLP(v), desc:"Ingresos menos costos y gastos", data: (() => { try { const sm=groupData(sales.filter(s=>s.date>=from&&s.date<=to),s=>s.date,s=>s.salePrice-(s.costPrice||0)); const em=groupData(expenses.filter(e=>e.date>=from&&e.date<=to),e=>e.date,e=>e.amount); const ks=[...new Set([...sm.map(d=>d.label),...em.map(d=>d.label)])].sort(); return ks.map(l=>({label:l,value:(sm.find(d=>d.label===l)?.value||0)-(em.find(d=>d.label===l)?.value||0)})); } catch{return[];} })() }, credito: { title:"Ganancias crédito", color:"#0ea5e9", icon:" ", fmtVal:v=>fmtCLP(v), desc:"Ganancia neta de ventas a crédito", data: groupData(approvedCredits, c=>c.approvedAt, c=>c.adminReceives-(c.costPrice||0)) }, retenciones: { title:"Total retenciones", color:"#a16207", icon:" ", fmtVal:v=>fmtCLP(v), desc:"Retenciones acumuladas de créditos", data: groupData((creditSales||[]).filter(c=>c.status==="approved"), c=>c.approvedAt, c=>c.retentionAmount) }, tarjeta: { title:"Ganancias tarjeta", color:"#0369a1", icon:" ", fmtVal:v=>fmtCLP(v), desc:"Ganancia neta de ventas con tarjeta", data: groupData(approvedCards, c=>c.approvedAt, c=>c.salePrice-(c.costPrice||0)) }, }; const cfg = chartConfigs[activeChart] || chartConfigs.ventas; const card = statCards.find(c=>c.key===activeChart)||statCards[0]; return ( <Page title="Reportes" sub="Análisis de ventas y gastos por período"> <ChartView title={cfg.title} color={cfg.color} icon={cfg.icon} onBack={()=>setActiveChart(null)} granularity={granularity} setGranularity={setGranularity} dateFilter={dateFilter} setDateFilter={setDateFilter}> {/* Summary stat */} <div style={{display:"flex",alignItems:"center",gap:16,marginBottom:16,flexWrap:"wrap"}}> <div style={{background:cfg.color+"18",border:`1px solid ${cfg.color}40`,borderRadius:10,padding:"10px 18px"}}> <div style={{fontSize:11,color:"#6b7280",fontWeight:600,textTransform:"uppercase",marginBottom:2}}>{card.sub}</div> <div style={{fontSize:22,fontWeight:800,color:cfg.color}}>{card.val}</div> </div> <div style={{fontSize:12,color:"#6b7280"}}>{cfg.desc}</div> </div> <BarChart data={cfg.data} color={cfg.color} labelFn={d=>fmtLabel(d.label)} valFn={d=>Math.abs(d.value)} fmtVal={cfg.fmtVal} /> {/* Detail table */} {cfg.data.length>0&&( <div style={{marginTop:16,overflowX:"auto"}}>


=== PAGE 48 ===
<table style={{...S.table,width:"100%"}}> <thead><tr style={S.thead}><th style={S.th}>Período</th><th style={S.th}>Valor</th></tr></thead> <tbody> {cfg.data.map((d,i)=>( <tr key={i} style={S.tr}> <td style={S.td}>{d.label}</td> <td style={S.td}><span style={{fontWeight:700,color:cfg.color}}>{cfg.fmtVal(d.value)}</span></td> </tr> ))} </tbody> </table> </div> )} </ChartView> </Page> ); } // ── Vista principal de reportes ──────────────────────────────────────── return ( <Page title="Reportes" sub="Haz clic en un recuadro para ver el gráfico detallado"> <div style={{display:"flex",gap:8,marginBottom:20,flexWrap:"wrap",alignItems:"flex-end"}}> <div><label style={S.label}>Desde</label><input type="date" style={{...S.input,width:"auto"}} value={from} onChange={e=>setFrom(e.target.value)}/></div> <div><label style={S.label}>Hasta</label><input type="date" style={{...S.input,width:"auto"}} value={to} onChange={e=>setTo(e.target.value)}/></div> </div> {/* Clickable stat cards */} <div style={S.statsGrid}> {statCards.map(c=>( <div key={c.key} onClick={()=>{setActiveChart(c.key);setGranularity("día");setDateFilter({day:from,from,to,year:new Date(from).getFullYear(),yearFrom:new Date(from).getFullYear()-4,yearTo:new Date(from).getFullYear()});}} style={{...S.statCard,borderTop:`3px solid ${c.color}`,cursor:"pointer",transition:"box-shadow .15s, transform .15s"}} onMouseEnter={e=>{e.currentTarget.style.boxShadow="0 4px 16px rgba(0,0,0,.12)";e.currentTarget.style.transform="translateY(-2px)";}} onMouseLeave={e=>{e.currentTarget.style.boxShadow="";e.currentTarget.style.transform="";}}> <span style={{fontSize:28}}>{c.icon}</span> <div style={{fontSize:22,fontWeight:800,color:c.color,lineHeight:1.1,marginTop:4}}>{c.val}</div> <div style={{fontSize:13,fontWeight:600,color:"#374151"}}>{c.label}</div> <div style={{fontSize:11,color:"#9ca3af",marginTop:2}}>{c.sub}</div> <div style={{fontSize:10,color:c.color,marginTop:4,fontWeight:600}}>Ver gráfico →</div> </div> ))} </div> {approvedCredits.length>0&&( <Card title="Ventas a Crédito Aprobadas" style={{marginBottom:16}}> <div style={S.tableWrap}> <table style={S.table}> <thead><tr style={S.thead}>{["Aprobado","Teléfono","Vendedor","Total","Costo Producto","Ganancia Crédito","Retención"].map(h=><th key={h} style={S.th}>{h}</th>)}</tr></thead> <tbody>


=== PAGE 49 ===
{approvedCredits.map(cr=>( <tr key={cr.id} style={S.tr}> <td style={S.td}>{cr.approvedAt}</td> <td style={S.td}><div style={{fontWeight:600,fontSize:13}}>{cr.productName}</div>{cr.imei&&<code style={{fontSize:10,color:"#9ca3af"}}>{cr.imei}</code>}</td> <td style={S.td}>{cr.requestedByName}</td> <td style={S.td}><span style={{color:"#7c3aed",fontWeight:800}}>{fmtCLP(cr.adminReceives)}</span></td> <td style={S.td}><span style={{color:"#6b7280"}}>{fmtCLP(cr.costPrice||0)}</span></td> <td style={S.td}><span style={{color:"#059669",fontWeight:700}}>{fmtCLP(cr.adminReceives-(cr.costPrice||0))}</span></td> <td style={S.td}><span style={{color:"#dc2626",fontWeight:600}}>{fmtCLP(cr.retentionAmount)}</span></td> </tr> ))} </tbody> </table> </div> </Card> )} {pendingCredits.length>0&&( <Card title={`Créditos Pendientes (${pendingCredits.length})`} style={{marginBottom:16}}> {pendingCredits.map(cr=>( <div key={cr.id} style={{...S.row,background:"#fffbeb",border:"1px solid #fde68a",borderRadius:8,padding:"8px 12px",marginBottom:4}}> <span style={{fontSize:13,fontWeight:600,flex:1,color:"#111827"}}>{cr.productName}</span> <span style={{fontSize:12,color:"#6b7280"}}>{cr.requestedByName}</span> <span style={{fontSize:12,fontWeight:700,color:"#7c3aed",marginLeft:12}}>{fmtCLP(cr.adminReceives)}</span> </div> ))} </Card> )} {approvedCards.length>0&&( <Card title="Ventas con Tarjeta Aprobadas" style={{marginBottom:16}}> <div style={S.tableWrap}> <table style={S.table}> <thead><tr style={S.thead}>{["Aprobado","Producto","Vendedor","Valor Producto","Valor con Recargo","Cuotas","Ganancia"].map(h=><th key={h} style={S.th}>{h}</th>)}</tr></thead> <tbody> {approvedCards.map(cd=>( <tr key={cd.id} style={S.tr}> <td style={S.td}>{cd.approvedAt}</td> <td style={S.td}><div style={{fontWeight:600,fontSize:13}}>{cd.productName}</div>{cd.imei&&<code style={{fontSize:10,color:"#9ca3af"}}>{cd.imei}</code>}</td> <td style={S.td}>{cd.requestedByName}</td> <td style={S.td}><span style={{color:"#059669",fontWeight:700}}>{fmtCLP(cd.salePrice)}</span></td> <td style={S.td}><span style={{color:"#2563eb",fontWeight:600}}>{fmtCLP(cd.finalPrice)}</span></td> <td style={S.td}>{cd.useCuotas?<span>{cd.meses} meses{cd.valorMes?<> · <strong>{fmtCLP(cd.valorMes)}/mes</strong></>:""}</span>:"—"}</td> <td style={S.td}><span style={{color:"#0369a1",fontWeight:700}}>{fmtCLP(cd.salePrice-(cd.costPrice||0))}</span></td> </tr> ))} </tbody> </table> </div>


=== PAGE 50 ===
</Card> )} <div style={S.twoCol}> {bySeller.length>0&&( <Card title="Por Vendedor"> {bySeller.map(u=>( <div key={u.id} style={S.row}> <div style={{...S.avatar,width:30,height:30,fontSize:13,flexShrink:0}}>{u.name[0]}</div> <span style={{flex:1,fontSize:13,color:"#374151",fontWeight:500}}>{u.name}</span> <Pill>{u.count} ventas</Pill> <span style={{fontSize:13,fontWeight:700,color:"#059669",minWidth:90,textAlign:"right"}}>{fmtCLP(u.rev)}</span> </div> ))} </Card> )} {byCat.length>0&&( <Card title="Por Categoría"> {byCat.map(c=>( <div key={c.id} style={S.row}> <span style={{fontSize:22}}>{c.icon}</span> <span style={{flex:1,fontSize:13,color:"#374151",fontWeight:500}}>{c.name}</span> <Pill>{c.count} uds.</Pill> <span style={{fontSize:13,fontWeight:700,color:"#059669"}}>{fmtCLP(c.rev)}</span> </div> ))} </Card> )} </div> {f.length>0&&( <Card title="Detalle" style={{marginTop:16}}> <div style={S.tableWrap}> <table style={S.table}> <thead><tr style={S.thead}>{["Fecha","Producto","Vendedor","Costo","Venta","Ganancia"].map(h=><th key={h} style={S.th}>{h}</th>)}</tr></thead> <tbody> {f.slice().reverse().map(s=>( <tr key={s.id} style={S.tr}> <td style={S.td}>{s.date}</td><td style={S.td}>{s.productName}</td><td style={S.td}>{s.userName}</td> <td style={S.td}>{fmtCLP(s.costPrice)}</td> <td style={S.td}><span style={{fontWeight:700,color:"#059669"}}>{fmtCLP(s.salePrice)}</span></td> <td style={S.td}><span style={{color:"#d97706",fontWeight:600}}>{fmtCLP(s.salePrice-s.costPrice)}</span></td> </tr> ))} </tbody> </table> </div> </Card> )}


=== PAGE 51 ===
</Page> ); } /* ─── PERSONAL ───────────────────────────────────────────────────────────── */ /* ─── Generador de credenciales desde nombre completo ─────────────────────── */ const genCredentials = (fullName, existingUsers=[]) => { // Normalizar: quitar tildes, minúsculas const normalize = s => s.normalize("NFD").replace(/[\u0300-\u036f]/g,"").toLowerCase().replace(/[^a-z0-9]/g,""); const parts = fullName.trim().split(/\s+/).filter(Boolean); const first = normalize(parts[0] || ""); const last = normalize(parts[1] || ""); const last2 = normalize(parts[2] || ""); // Usuario: primera letra del nombre + apellido (máx 10 chars) const baseUser = (first.slice(0,1) + (last||first.slice(1,6))).slice(0,10); // Asegurar unicidad let username = baseUser; let counter = 2; while (existingUsers.some(u => u.username === username)) { username = baseUser.slice(0,8) + counter++; } // Contraseña: 2 letras del primer nombre + 2 letras del apellido + 3 dígitos derivados const letters = (first.slice(0,2) + (last||first.slice(2,4)) + (last2.slice(0,1)||"")).slice(0,4); // Número derivado del nombre (no aleatorio puro — reproducible y memorable) const seed = fullName.split("").reduce((a,c)=>a+c.charCodeAt(0),0); const num3 = String(((seed * 31) % 900) + 100); // siempre 3 dígitos 100-999 const password = letters + num3; // ej: "mago342" return { username, password }; }; function Staff({users,setUsers,notify}) { const [modal,setModal]=useState(false); const [form,setForm]=useState({}); const [showPass,setShowPass]=useState(false); const [copied,setCopied]=useState(false); const open = (u) => { if (u) { setForm({...u}); } else { setForm({name:"",username:"",password:"",role:"vendedor",active:true}); }


=== PAGE 52 ===
setShowPass(false); setCopied(false); setModal(true); }; // Regenerar credenciales cuando cambia el nombre (solo en creación) const handleNameChange = (name) => { if (form.id) { setForm({...form,name}); return; } if (name.trim().split(/\s+/).filter(Boolean).length >= 2) { const creds = genCredentials(name, users); setForm(f=>({...f, name, username:creds.username, password:creds.password})); } else { setForm(f=>({...f, name, username:"", password:""})); } }; const copyCredentials = () => { const text = `Usuario: ${form.username}\nContraseña: ${form.password}`; navigator.clipboard?.writeText(text).catch(()=>{}); setCopied(true); setTimeout(()=>setCopied(false),2000); }; const save = () => { if (!form.name||!form.username||!form.password) { notify("Completa todos los campos","err"); return; } if (form.id) setUsers(users.map(u=>u.id===form.id?form:u)); else setUsers([...users,{...form,id:"u"+Date.now()}]); notify(form.id?"Usuario actualizado":"Usuario creado"); setModal(false); }; return ( <Page title="Personal" sub={`${users.length} usuarios registrados`} action={<button style={S.btnPrimary} onClick={()=>open(null)}>+ Nuevo Usuario</button>}> <div style={{display:"grid",gridTemplateColumns:"repeat(auto-fill,minmax(270px,1fr))",gap:14}}> {users.map(u=>( <div key={u.id} style={{background:"#fff",border:"1px solid #e5e7eb",borderRadius:14,padding:18,opacity:u.active?1:.6}}> <div style={{display:"flex",gap:12,alignItems:"center",marginBottom:12}}> <div style={{...S.avatar,width:44,height:44,fontSize:18,background:ROLES[u.role].bg,color:ROLES[u.role].color}}>{u.name[0]}</div> <div> <div style={{fontWeight:700,color:"#111827"}}>{u.name}</div> <code style={{...S.mono,fontSize:11,color:"#9ca3af"}}>@{u.username}</code> </div> </div> <div style={{display:"flex",gap:8,marginBottom:10}}> <span style={{...S.rolePill,background:ROLES[u.role].bg,color:ROLES[u.role].color,flex:1,textAlign:"center"}}>{ROLES[u.role].label}</span> <span style={{...S.rolePill,background:u.active?"#ecfdf5":"#f3f4f6",color:u.active?"#059669":"#9ca3af"}}>{u.active?"Activo":"Inactivo"}</span> </div> {u.role!=="admin"&&u.scheduleStart&&u.scheduleEnd&&( <div style={{fontSize:11,color:"#6b7280",background:"#f0f9ff",borderRadius:6,padding:"4px 8px",marginBottom:8,display:"inline-block"}}> {u.scheduleStart} – {u.scheduleEnd}


=== PAGE 53 ===
</div> )} <div style={{display:"flex",gap:6}}> <Btn onClick={()=>open(u)}>Editar</Btn> <Btn color={u.active?"#d97706":"#059669"} onClick={()=>setUsers(users.map(x=>x.id===u.id?{...x,active:!x.active}:x))}> {u.active?"Desactivar":"Activar"} </Btn> </div> </div> ))} </div> {modal&&( <Modal title={form.id?"Editar Usuario":"Nuevo Usuario"} onClose={()=>setModal(false)}> <div style={S.fgrid}> {/* Nombre — dispara la generación automática */} <Fld label="Nombre completo *" full> <input style={S.input} value={form.name} placeholder="ej: María González Rojas" onChange={e=>handleNameChange(e.target.value)}/> {!form.id&&form.name.trim().split(/\s+/).filter(Boolean).length<2&&form.name&&( <div style={{fontSize:11,color:"#d97706",marginTop:4}}>Ingresa al menos nombre y apellido para generar credenciales</div> )} </Fld> {/* Credenciales — auto-generadas y editables */} <Fld label="Usuario (login)"> <input style={S.input} value={form.username} onChange={e=>setForm({...form,username:e.target.value.toLowerCase().replace(/\s/g,"")})}/> </Fld> <Fld label="Contraseña"> <div style={{position:"relative"}}> <input style={{...S.input,paddingRight:40,fontFamily:"'JetBrains Mono',monospace",letterSpacing:".05em"}} type={showPass?"text":"password"} value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/> <button onClick={()=>setShowPass(s=>!s)} style={{position:"absolute",right:8,top:"50%",transform:"translateY(-50%)",background:"none",border:"none",cursor:"pointer",fontSize:14,color:"#9ca3af",lineHeight:1}}> {showPass?" ":" "} </button> </div> </Fld> <Fld label="Rol" full> <select style={S.select} value={form.role} onChange={e=>setForm({...form,role:e.target.value})}> {Object.entries(ROLES).map(([k,v])=><option key={k} value={k}>{v.label}</option>)} </select>


=== PAGE 54 ===
</Fld> {/* Horario — solo aplica a no-admins */} {form.role!=="admin"&&( <> <Fld label="Horario inicio (HH:MM)"> <input style={S.input} type="time" value={form.scheduleStart||""} onChange={e=>setForm({...form,scheduleStart:e.target.value})}/> </Fld> <Fld label="Horario fin (HH:MM)"> <input style={S.input} type="time" value={form.scheduleEnd||""} onChange={e=>setForm({...form,scheduleEnd:e.target.value})}/> </Fld> <Fld label="" full> <div style={{fontSize:11,color:"#6b7280",background:"#f9fafb",padding:"7px 10px",borderRadius:8}}> Si se define horario, el vendedor no podrá ingresar a la plataforma fuera de ese rango. Deja vacío para sin restricción. </div> </Fld> </> )} </div> {/* Panel de credenciales generadas */} {form.username&&form.password&&( <div style={{marginTop:14,background:"#f0fdf4",border:"1px solid #86efac",borderRadius:10,padding:"12px 14px"}}> <div style={{fontSize:11,fontWeight:700,color:"#166534",textTransform:"uppercase",letterSpacing:".05em",marginBottom:8}}> Credenciales {form.id?"actuales":"generadas"} </div> <div style={{display:"flex",gap:12,flexWrap:"wrap",marginBottom:8}}> <div style={{flex:1}}> <div style={{fontSize:10,color:"#6b7280",marginBottom:2}}>USUARIO</div> <code style={{fontFamily:"'JetBrains Mono',monospace",fontSize:14,fontWeight:700,color:"#1d4ed8"}}>{form.username}</code> </div> <div style={{flex:1}}> <div style={{fontSize:10,color:"#6b7280",marginBottom:2}}>CONTRASEÑA</div> <code style={{fontFamily:"'JetBrains Mono',monospace",fontSize:14,fontWeight:700,color:"#059669"}}> {showPass ? form.password : "•".repeat(form.password.length)} </code> </div> </div> <button onClick={copyCredentials} style={{background:copied?"#dcfce7":"#fff",border:`1px solid ${copied?"#86efac":"#d1d5db"}`,borderRadius:6,padding:"5px 12px",fontSize:12,color:copied?"#166534":"#374151",cursor:"pointer",fontFamily:"inherit",fontWeight:500,transition:"all .2s"}}> {copied?"✓ ¡Copiado!":" Copiar credenciales"} </button> </div> )} <div style={{marginTop:10,padding:"10px 12px",background:"#eff6ff",borderRadius:8,fontSize:12,color:"#1d4ed8"}}> <strong>Permisos:</strong> {form.role==="admin"?"Acceso completo: inventario, ventas, reportes, personal":"Solo registro de ventas y consulta de stock"}


=== PAGE 55 ===
</div> <div style={{display:"flex",gap:8,marginTop:14}}> <button style={S.btnPrimary} onClick={save}>Guardar</button> <button style={S.btnGhost} onClick={()=>setModal(false)}>Cancelar</button> </div> </Modal> )} </Page> ); } /* ─── UI ATOMS ───────────────────────────────────────────────────────────── */ function Page({title,sub,action,children}) { return ( <div style={{padding:"24px 28px",maxWidth:1080,margin:"0 auto"}} className="page-pad"> <div style={{display:"flex",justifyContent:"space-between",alignItems:"flex-start",marginBottom:20,flexWrap:"wrap",gap:10}}> <div><h1 style={{fontSize:22,fontWeight:800,color:"#111827",letterSpacing:"-.03em",margin:0}}>{title}</h1> {sub&&<p style={{fontSize:13,color:"#9ca3af",marginTop:4,margin:0}}>{sub}</p>}</div> {action&&<div>{action}</div>} </div> {children} </div> ); } function Card({title,children,style:ex}) { return <div style={{...S.card,...ex}}>{title&&<div style={S.cardTitle}>{title}</div>}{children}</div>; } function Modal({title,children,onClose}) { return ( <div style={S.overlay} onClick={e=>e.target===e.currentTarget&&onClose()}> <div style={S.modalBox}> <div style={{display:"flex",justifyContent:"space-between",alignItems:"center",padding:"16px 20px",borderBottom:"1px solid #f3f4f6"}}> <span style={{fontWeight:700,color:"#111827",fontSize:16}}>{title}</span> <button style={{background:"none",border:"none",cursor:"pointer",fontSize:22,color:"#9ca3af",lineHeight:1}} onClick={onClose}>×</button> </div> <div style={{padding:"18px 20px",overflowY:"auto"}}>{children}</div> </div> </div> ); } function Toast({msg,type}) { const map={ok:["#ecfdf5","#059669","#d1fae5"],err:["#fef2f2","#dc2626","#fecaca"],warn:["#fffbeb","#d97706","#fde68a"]}; const [bg,cl,br]=map[type]||map.ok; return <div style={{position:"fixed",top:16,right:16,padding:"12px 18px",borderRadius:10,fontWeight:600,fontSize:13,zIndex:999,background:bg,color:cl,border:`1px solid ${br}`,boxShadow:"0 4px 20px #0002",animation:"toastIn .3s ease"}}>{type==="err"?" ":type==="warn"?" ":"✓"} {msg}</div>; } function StockBadge({n,alert,sm}) { const [bg,cl]=n===0?["#fef2f2","#dc2626"]:n<=alert?["#fffbeb","#d97706"]:["#ecfdf5","#059669"];


=== PAGE 56 ===
return <span style={{background:bg,color:cl,borderRadius:6,padding:sm?"2px 6px":"3px 8px",fontSize:11,fontWeight:700,whiteSpace:"nowrap"}}>{n} uds.</span>; } function Pill({children,color="#e5e7eb",tc="#6b7280"}) { return <span style={{background:color,color:tc,borderRadius:20,padding:"3px 10px",fontSize:11,fontWeight:600,whiteSpace:"nowrap"}}>{children}</span>; } function Btn({children,onClick,color="#374151"}) { return <button onClick={onClick} style={{background:"#f9fafb",border:"1px solid #e5e7eb",borderRadius:6,padding:"4px 10px",fontSize:12,color,cursor:"pointer",fontWeight:500,fontFamily:"inherit"}}>{children}</button>; } function Fld({label,children,full}) { return <div style={{gridColumn:full?"1 / -1":"auto"}}>{label&&<label style={S.label}>{label}</label>}{children}</div>; } /* ─── ESTILOS ─────────────────────────────────────────────────────────────── */ const FF="'Plus Jakarta Sans','Segoe UI',sans-serif"; const S = { root: {display:"flex",height:"100vh",background:"#f5f4f0",fontFamily:FF,overflow:"hidden"}, sidebar: {background:"#fff",flexDirection:"column",padding:"20px 12px",gap:4,borderRight:"1px solid #e5e7eb",flexShrink:0,overflowY:"auto",overflowX:"hidden"}, brand: {display:"flex",alignItems:"center",gap:10,padding:"0 6px 14px",borderBottom:"1px solid #f3f4f6",marginBottom:8}, brandName:{fontWeight:800,fontSize:15,color:"#111827",letterSpacing:"-.02em"}, brandSub: {fontSize:10,color:"#9ca3af",marginTop:1}, userCard: {display:"flex",alignItems:"center",gap:10,padding:"10px",background:"#f9fafb",borderRadius:10,marginBottom:12}, avatar: {width:36,height:36,borderRadius:"50%",background:"#dbeafe",color:"#1d4ed8",display:"flex",alignItems:"center",justifyContent:"center",fontWeight:800,fontSize:15,flexShrink:0}, rolePill: {fontSize:10,fontWeight:600,padding:"2px 8px",borderRadius:20,display:"inline-block"}, navBtn: {display:"flex",alignItems:"center",gap:10,padding:"9px 12px",borderRadius:8,border:"none",background:"transparent",color:"#6b7280",cursor:"pointer",textAlign:"left",fontSize:13,fontWeight:500,fontFamily:FF,transition:"all .15s",width:"100%"}, navActive:{background:"#eff6ff",color:"#1d4ed8",fontWeight:700}, logoutBtn:{background:"none",border:"none",color:"#9ca3af",fontSize:12,cursor:"pointer",padding:"10px 12px",textAlign:"left",fontFamily:FF,marginTop:"auto",width:"100%"}, toggleBtn:{background:"#f3f4f6",border:"1px solid #e5e7eb",borderRadius:7,cursor:"pointer",padding:"4px 8px",fontSize:12,color:"#6b7280",flexShrink:0,lineHeight:1.4,fontFamily:FF}, // Mobile mobileTopBar:{position:"sticky",top:0,zIndex:50,background:"#fff",borderBottom:"1px solid #e5e7eb",padding:"10px 16px",alignItems:"center",justifyContent:"space-between",gap:12}, mobileMenuBtn:{background:"none",border:"1px solid #e5e7eb",borderRadius:8,padding:"6px 10px",fontSize:18,cursor:"pointer",color:"#374151",lineHeight:1}, mobileNav:{position:"fixed",bottom:0,left:0,right:0,zIndex:50,background:"#fff",borderTop:"1px solid #e5e7eb",padding:"6px 0 env(safe-area-inset-bottom,6px)",justifyContent:"space-around",alignItems:"center",boxShadow:"0 -2px 12px #0001"}, mobileNavBtn:{display:"flex",flexDirection:"column",alignItems:"center",gap:1,padding:"4px 12px",background:"none",border:"none",cursor:"pointer",color:"#9ca3af",fontFamily:FF,minWidth:52}, mobileNavActive:{color:"#1d4ed8"}, drawerOverlay:{position:"fixed",inset:0,background:"#00000060",zIndex:300,display:"flex"}, drawer:{width:260,background:"#fff",height:"100%",display:"flex",flexDirection:"column",padding:"20px 14px",gap:4,overflowY:"auto",animation:"slideRight .22s ease",boxShadow:"4px 0 24px #0002"}, main: {flex:1,overflowY:"auto",background:"#f5f4f0",display:"flex",flexDirection:"column"}, card: {background:"#fff",borderRadius:14,padding:"18px 20px",border:"1px solid #e5e7eb"}, cardTitle:{fontSize:11,fontWeight:700,color:"#9ca3af",textTransform:"uppercase",letterSpacing:".08em",marginBottom:14}, statsGrid:{display:"grid",gridTemplateColumns:"repeat(auto-fill,minmax(160px,1fr))",gap:12,marginBottom:20}, statCard: {background:"#fff",borderRadius:14,padding:"18px 16px",border:"1px solid #e5e7eb",display:"flex",flexDirection:"column",gap:4}, twoCol: {display:"grid",gridTemplateColumns:"repeat(auto-fill,minmax(300px,1fr))",gap:16}, row: {display:"flex",alignItems:"center",gap:8,padding:"8px 0",borderBottom:"1px solid #f3f4f6"}, tableWrap:{overflowX:"auto"}, table: {width:"100%",borderCollapse:"collapse",minWidth:480}, thead: {background:"#f9fafb"}, th: {padding:"8px 12px",fontSize:11,fontWeight:700,color:"#9ca3af",textAlign:"left",textTransform:"uppercase",letterSpacing:".06em",whiteSpace:"nowrap"}, td: {padding:"11px 12px",borderBottom:"1px solid #f3f4f6",fontSize:13,color:"#6b7280",verticalAlign:"middle"},


=== PAGE 57 ===
tr: {transition:"background .1s"}, input: {background:"#fff",border:"1px solid #d1d5db",borderRadius:8,padding:"8px 12px",fontSize:13,color:"#111827",width:"100%",fontFamily:FF,transition:"border .15s"}, select: {background:"#fff",border:"1px solid #d1d5db",borderRadius:8,padding:"8px 12px",fontSize:13,color:"#111827",width:"100%",fontFamily:FF}, label: {display:"block",fontSize:11,fontWeight:700,color:"#6b7280",marginBottom:5,textTransform:"uppercase",letterSpacing:".05em"}, fgrid: {display:"grid",gridTemplateColumns:"1fr 1fr",gap:"14px 16px"}, empty: {color:"#9ca3af",fontSize:13,padding:"8px 0"}, btnPrimary:{background:"#1d4ed8",color:"#fff",border:"none",borderRadius:8,padding:"9px 18px",fontSize:13,fontWeight:600,cursor:"pointer",fontFamily:FF}, btnGhost: {background:"#fff",color:"#374151",border:"1px solid #d1d5db",borderRadius:8,padding:"8px 16px",fontSize:13,fontWeight:500,cursor:"pointer",fontFamily:FF}, overlay: {position:"fixed",inset:0,background:"#0000004d",display:"flex",alignItems:"center",justifyContent:"center",zIndex:200,padding:16}, modalBox: {background:"#fff",borderRadius:16,width:"100%",maxWidth:560,maxHeight:"92vh",display:"flex",flexDirection:"column",boxShadow:"0 20px 60px #0002"}, tabs: {display:"flex",gap:4,marginBottom:16,background:"#f3f4f6",padding:4,borderRadius:10,flexWrap:"wrap"}, tab: {background:"transparent",border:"none",borderRadius:7,padding:"6px 14px",fontSize:13,fontWeight:500,color:"#6b7280",cursor:"pointer",fontFamily:FF}, tabOn: {background:"#fff",color:"#111827",fontWeight:700,boxShadow:"0 1px 3px #0001"}, mono: {fontFamily:"'JetBrains Mono',monospace",background:"#f3f4f6",padding:"2px 6px",borderRadius:5}, catTag: {background:"#f0f9ff",color:"#0369a1",fontSize:11,fontWeight:600,padding:"3px 8px",borderRadius:20}, loginBg: {minHeight:"100vh",display:"flex",background:"#f5f4f0",fontFamily:FF}, loginWrap:{display:"flex",width:"100%",flexWrap:"wrap"}, loginLeft:{flex:"1 1 280px",background:"#1d4ed8",display:"flex",flexDirection:"column",justifyContent:"center",padding:"48px 40px",minHeight:300}, loginRight:{flex:"1 1 280px",display:"flex",alignItems:"center",justifyContent:"center",padding:"32px 20px"}, loginCard:{background:"#fff",borderRadius:18,padding:"36px 28px",width:"100%",maxWidth:400,boxShadow:"0 4px 40px #0001"}, loginBig: {fontSize:36,fontWeight:800,color:"#fff",letterSpacing:"-.04em",margin:"10px 0 8px"}, };
